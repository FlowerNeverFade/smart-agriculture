{
  "name": "agriloop-web-ui",
  "private": true,
  "version": "0.1.0",
  "description": "AgriLoop static web interface",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3000",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 3000"
  },
  "devDependencies": {
    "vite": "8.2.2"
  }
}
