# Bundled WebGL runtime

`three.module.js` is the browser ESM build of Three.js `0.180.0`, bundled so the
optional visual login page works on the static Nginx deployment without a CDN or
an installed `node_modules` directory. The accompanying `THREE-LICENSE` file is
the license shipped by Three.js.
