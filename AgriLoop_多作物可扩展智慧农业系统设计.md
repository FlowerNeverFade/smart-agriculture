# AgriLoop 多作物可扩展智慧农业系统设计

> 目标：在现有智慧农业实训要求基础上，将系统从“针对单一作物的农业 Demo”升级为“面向多作物、可持续扩展、可稳定维护的农业智能决策平台”。

---

## 1. 设计目标

AgriLoop 不为每一种农作物分别开发一套独立系统，而是采用：

> **统一农业智能平台 + 可插拔 Crop Pack（作物知识包）**

其中：

- 数据采集、MQTT 传输、数据库、告警、设备控制、Agent、权限管理和可视化等能力统一复用；
- 不同农作物之间的差异，通过 Crop Pack 配置；
- 新增作物时，原则上只需要新增一个 Crop Pack，而不需要重新开发后端、前端或 Agent。

整体结构：

```text
                         AgriLoop
                            │
               ┌────────────┴────────────┐
               │                         │
           通用系统内核               作物中心
               │                         │
     数据 / 告警 / 控制 / UI        Crop Registry
     数据库 / Agent / 审计              │
                                   ┌─────┼─────┐
                                  番茄  水稻  玉米 ...
                                   │
                              Crop Pack
```

---

## 2. Crop Pack：每种作物的专业能力载体

每一种作物对应一个 Crop Pack。Crop Pack 不是单纯的 RAG 文档集合，而是该作物在系统中的完整专业配置。

建议每个 Crop Pack 至少包含以下 8 个部分。

### 2.1 作物档案 Identity

用于描述作物的基础信息：

- 作物名称；
- 学名；
- 品种；
- 作物类型；
- 种植环境；
- 地区；
- 版本信息。

例如：

```yaml
crop: tomato
name: 番茄
scientificName: Solanum lycopersicum
cultivar: demo-default
environment: greenhouse
region: southwest-china
version: 1.0
```

---

### 2.2 生长阶段 Growth Stage

系统不能只识别“这是什么作物”，还需要知道“当前处于什么生长阶段”。

例如番茄：

```text
苗期
→ 营养生长期
→ 开花期
→ 坐果期
→ 果实膨大期
→ 成熟期
```

不同阶段可以拥有不同的：

- 环境目标；
- 风险规则；
- 灌溉要求；
- RAG 知识范围；
- 决策策略。

因此，系统真正分析的是：

> **地块 + 作物 + 生长阶段 + 当前环境**

而不是只分析“番茄”或“水稻”。

---

### 2.3 指标配置 Metric Profile

不同作物、不同阶段关注的指标可能不同，因此指标不能在系统中写死。

统一指标体系可以包含：

```text
SOIL_MOISTURE      土壤湿度
AIR_TEMPERATURE    空气温度
AIR_HUMIDITY       空气湿度
LIGHT              光照
CO2                二氧化碳
SOIL_TEMPERATURE   土壤温度
PH                 pH
EC                 电导率
WATER_LEVEL        水层深度
LEAF_WETNESS       叶面湿度
...
```

不同 Crop Pack 只负责声明当前需要哪些指标。

例如：

```text
番茄：
土壤湿度
空气温度
空气湿度
光照
CO₂
```

```text
水稻：
水层深度
土壤湿度
空气温度
光照
```

这样新增作物或新增传感器时，无需重构整个系统。

---

### 2.4 规则包 Rule Pack

关键判断不能完全依赖大模型。

Crop Pack 应包含确定性的农业规则，例如：

```text
土壤湿度连续低于目标范围
AND
持续时间 > 10 分钟
AND
传感器可信度 > 0.8
→ 缺水风险
```

规则可以覆盖：

- 缺水；
- 过湿；
- 高温；
- 低温；
- 光照异常；
- 复合风险；
- 传感器异常；
- 设备异常；
- 灌溉冷却窗口等。

规则系统负责“是否异常”，LLM 不负责凭空决定阈值。

---

### 2.5 RAG 知识包 Knowledge Pack

RAG 用于提供专业农业知识与解释依据。

知识建议分层组织，而不是将所有农业资料混入一个知识库。

```text
通用农业知识
      ↓
作物知识
      ↓
生长阶段知识
      ↓
地区 / 种植环境知识
      ↓
具体地块历史知识
```

例如对“重庆温室番茄结果期”进行分析时，优先检索：

```text
crop = tomato
stage = fruiting
region = southwest-china
environment = greenhouse
```

如果没有足够资料，则逐级回退：

```text
重庆温室番茄
→ 西南温室番茄
→ 温室番茄
→ 番茄
→ 通用农业知识
```

通过这种方式减少跨作物、跨阶段错误检索。

---

### 2.6 决策策略 Decision Policy

Crop Pack 还应提供执行约束，例如：

- 推荐灌溉量；
- 推荐灌溉时间；
- 最大灌溉上限；
- 冷却时间；
- 风险等级；
- 是否需要人工审批；
- 特定状态下是否禁止自动执行。

这部分与安全策略共同决定：

> AI 给出的建议是否可以转化为实际控制动作。

---

### 2.7 情景包 Scenario Pack

为了在没有真实农田变化的情况下稳定测试和答辩，每种作物可以配置典型模拟情景：

- normal：正常；
- drought：干旱；
- heat-wave：热浪；
- heavy-rain：暴雨；
- sensor-drift：传感器漂移；
- device-offline：设备离线。

情景模拟器生成数据后，必须继续走真实系统的同一条数据链路：

```text
模拟数据
→ MQTT
→ 后端
→ 数据库
→ 规则
→ Agent
→ 控制
→ 反馈
```

不能在模拟器中直接写死最终结果。

---

### 2.8 测试包 Test Pack

每个 Crop Pack 必须配套固定测试案例。

例如番茄：

```text
T001 正常状态
T002 轻度缺水
T003 严重缺水
T004 高温但水分正常
T005 湿度异常但传感器可信度低
T006 设备离线
T007 暴雨后过湿
...
```

每次修改：

- Agent；
- Prompt；
- RAG；
- Crop Pack；
- 农业规则；

都重新执行所有测试。

因此，“支持更多作物”不会简单等于“增加更多不可控 Bug”。

---

## 3. Agent 采用统一框架，而不是一种作物一个 Agent

不建议采用：

```text
番茄 Agent
玉米 Agent
水稻 Agent
辣椒 Agent
...
```

这种方案会造成大量重复的：

- Prompt；
- 工具；
- RAG 配置；
- 测试；
- Bug；
- 维护工作。

更合理的方案是：

> **同一个 Agent 框架 + 不同 Crop Pack = 不同作物专家**

统一 Agent Prompt 可以接收动态上下文：

```text
当前作物：
{CropProfile}

当前阶段：
{GrowthStage}

实时数据：
{Telemetry}

历史趋势：
{History}

规则结果：
{RuleResult}

知识证据：
{RetrievedKnowledge}

当前设备状态：
{DeviceStatus}
```

然后进行综合分析。

---

## 4. Agent 的标准工作流程

Agent 不应该在收到问题后直接让大模型自由回答。

标准流程应为：

```text
用户问题 / 告警事件
        ↓
① 确认地块
        ↓
② 确认作物
        ↓
③ 确认当前生长阶段
        ↓
④ 查询实时传感器
        ↓
⑤ 查询历史趋势
        ↓
⑥ 加载对应 Crop Pack
        ↓
⑦ 执行确定性规则
        ↓
⑧ 定向 RAG 检索
        ↓
⑨ LLM Agent 综合分析与解释
        ↓
⑩ 安全策略检查
        ↓
⑪ 返回建议 / 请求人工确认
        ↓
⑫ 执行虚拟或真实设备
        ↓
⑬ 结果反馈与决策回放
```

核心原则：

> **LLM 是综合分析层，不是整个系统唯一的判断来源。**

---

## 5. 四类能力分工

系统采用四类能力协同。

### 5.1 Rule Engine

负责确定性判断：

> 当前指标是否异常？

---

### 5.2 RAG

负责知识依据：

> 为什么这种状态值得关注？农业资料如何解释？

---

### 5.3 LLM Agent

负责综合推理和表达：

> 当前实时数据、历史趋势、农业知识和规则结果综合起来意味着什么？

---

### 5.4 Safety Policy

负责动作控制：

> 这个建议是否允许执行？是否必须经过人工确认？

因此：

```text
实时数据
   +
确定性规则
   +
RAG 专业知识
   +
LLM Agent
   +
安全策略
   ↓
农业决策
```

相比“所有事情都让大模型决定”，这种架构更稳定、更可解释。

---

## 6. 参数采用分层继承机制

农业实际场景存在明显的地区和地块差异，因此不能只有一套全局参数。

建议采用：

```text
系统默认值
    ↓
Crop Pack 默认值
    ↓
农场自定义值
    ↓
地块自定义值
```

例如：

```text
系统默认湿度下限：25%
番茄结果期默认：30%
A01 地块自定义：28%

最终 A01 使用：28%
```

这样既能保证标准化，又允许实际农场调整。

---

## 7. 数据层保持统一

无论种植什么作物，底层实时数据使用统一数据结构。

例如：

```text
Telemetry

plotId
deviceId
metricCode
value
unit
timestamp
quality
scenarioId
```

Crop Pack 只决定：

- 哪些 metricCode 对当前作物重要；
- 合理范围是什么；
- 如何解释这些指标。

这样番茄、水稻、玉米可以共用：

- MQTT；
- 后端；
- 数据库；
- 前端实时通道；
- Agent 工具；
- 告警系统。

这是平台能够长期扩展的基础。

---

## 8. 前端采用配置驱动

页面不为每一种作物分别开发。

例如进入番茄地块时，系统根据 Tomato Crop Pack 自动显示：

```text
土壤湿度
温度
空气湿度
光照
CO₂
```

进入水稻地块时自动切换为：

```text
水层深度
土壤湿度
温度
光照
```

因此新增作物原则上不需要重新写页面。

---

## 9. 数据可用性必须显式管理

Crop Pack 可以定义很多农业指标，但真实硬件不一定全部支持。

因此每个指标必须具有状态：

```text
SUPPORTED
当前真实设备可获得

SIMULATION_ONLY
当前仅模拟态存在

UNAVAILABLE
当前无数据
```

Agent 不允许对缺失数据进行猜测。

例如当前没有 EC 传感器时，应明确回答：

> 当前缺少 EC 数据，无法对该指标进行确定判断。

而不是生成虚假的 EC 数值。

---

## 10. Crop Pack 与知识版本化

所有 Crop Pack 必须具有版本号。

例如：

```text
Tomato Pack v1.0
Tomato Pack v1.1
```

每次 Agent 决策记录：

```text
cropPackVersion
ruleVersion
knowledgeVersion
agentVersion
traceId
```

这样未来可以追溯：

- 当时使用了什么实时数据；
- 使用了哪一版规则；
- 检索了什么知识；
- Agent 为什么得到该结论；
- 谁批准了动作；
- 最后执行结果如何。

这与 AgriLoop 当前规划的决策账本和回放机制结合后，可以形成完整的可审计决策链。

---

## 11. “大而稳”的核心原则

整个多作物系统需要长期坚持以下原则。

| 原则 | 含义 |
|---|---|
| 一个核心系统 | 不为每种作物重复开发系统 |
| Crop Pack 插件化 | 作物差异配置化 |
| 指标动态化 | 指标不能写死 |
| RAG 分层化 | 通用 → 作物 → 阶段 → 地区 → 地块 |
| 规则确定性 | 关键阈值判断不能完全依赖 LLM |
| Agent 统一化 | 同一框架动态加载 Crop Pack |
| 参数可覆盖 | 系统 → 作物 → 农场 → 地块 |
| 知识版本化 | 每次决策可以追溯 |
| 测试自动化 | 每个 Crop Pack 都必须有回归测试 |
| 数据可用性感知 | 缺失数据不能由 Agent 猜测 |

---

## 12. 第一阶段建议范围

系统架构应该支持大量作物，但第一版不建议为了数量一次录入几十种作物。

更合理的方案是选择约 6–8 种代表性作物，高质量完成 Crop Pack。

例如覆盖：

- 茄果类；
- 叶菜类；
- 瓜类；
- 谷物类；
- 豆类；
- 块根 / 块茎类。

每种示范作物都完整包含：

```text
生长阶段
+ 指标配置
+ 农业规则
+ RAG 知识
+ 决策策略
+ 情景模拟
+ 自动测试
```

这样可以证明：

> 系统并不是针对番茄硬编码，而是真正具有跨作物扩展能力。

---

## 13. 与 AgriLoop 原有闭环的关系

多作物设计不会改变 AgriLoop 当前核心闭环。

原有流程仍然是：

```text
感知
↓
数据传输
↓
异常判断
↓
知识检索
↓
Agent 分析
↓
安全决策
↓
人工确认 / 自动放行
↓
设备执行
↓
状态反馈
↓
决策回放
```

区别只是系统会在分析过程中自动加载当前作物对应的 Crop Pack。

因此，多作物能力属于对现有 AgriLoop 架构的增强，而不是推倒重做。

---

## 14. 最终系统定位

AgriLoop 最终不应定位为：

> “针对某一种农作物的智慧农业 Demo”

而应定位为：

> **面向多作物的可扩展农业智能决策平台。**

核心能力是：

> **统一的数据、设备、Agent 和可视化底座，通过 Crop Pack 动态注入不同作物、不同生长阶段、不同地区和不同地块的专业指标、规则和知识。**

最终形成：

```text
作物识别
   ↓
阶段识别
   ↓
动态指标
   ↓
实时感知
   ↓
确定性规则
   ↓
分层 RAG
   ↓
Agent 综合分析
   ↓
安全决策
   ↓
执行
   ↓
效果反馈
   ↓
审计与回放
```

这种设计既满足当前实训“模拟数据 + 智能体 + 可视化”的软件阶段要求，也可以在后续接入鸿蒙开发板和真实传感器时保持核心软件架构不变。

---

## 15. 一句话总结

> **AgriLoop 用一套统一智慧农业内核承载所有作物，用 Crop Pack 描述每种作物的阶段、指标、规则、知识、策略和测试，从而实现“新增作物主要靠配置，不靠重写代码”，最终构建一个既能扩展到多作物，又能保持稳定、可解释、可验证的农业智能决策平台。**
