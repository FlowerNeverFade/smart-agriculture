# SysAdminUI (SAU) 分支合并报告与功能说明

本报告旨在帮助队长在合并 SysAdminUI 分支至 main 分支时，快速梳理我们所做的核心修复与体验优化。我们在 SAU 分支上进行了大量针对**系统管理员视角**的前端与后端优化，请在 Code Review 和合并时重点关注以下内容。

## 一、 前端核心优化与修复

### 1. 平台总览页面 (Platform Overview)
* **定位调整与标题精简**：去除了“全局地块态势”中的“全局”二字，使其更贴合系统管理员的宏观定位。
* **异常驱动的默认视图**：当接入地块较多时，默认隐藏健康的正常地块，**仅展示告警 (CRITICAL)、预警 (WARNING) 和离线 (OFFLINE) 状态的异常地块**。
* **交互式状态统计条**：将原有的状态下拉框替换为了顶部的**胶囊状可交互过滤器 (Capsule Filters)**。点击对应的状态（如“告警 2”），下方网格会立刻过滤出相应的卡片。
* **打通排查闭环 (Drill-down)**：去除了点击地块卡片时无意义的“修改监测配置”弹窗。现在点击异常地块，会立刻带有 plotId 参数**路由跳转到“运行监控 (admin-ops)”**，实现一键定位设备和告警上下文。
* **网格布局优化**：使用 uto-fill 替代了 uto-fit，并调整了卡片的最小宽度，防止在异常地块较少（如只有两个）时卡片被无限拉伸变形。

### 2. 仿真验证界面 (Simulation Validation)
* **基于地块的精细化场景控制**：扩展了模拟器的控制粒度。支持针对具体地块选择独立的突发场景（如干旱、暴雨、设备离线）。
* **场景选择器联动与批量应用**：添加了全局场景下拉表，且全局场景选择器能够智能感知和响应单地块的场景设置，支持灵活的批量状态同步应用。

### 3. 作物包规则与版本 - 瀑布流布局 (Crop Pack Masonry Layout)
* **发布状态修复**：修复了“草莓”等作物包由草稿转为发布后，仍显示“暂不可用于新地块”的数据不同步问题。
* **响应式纯 CSS/Vue 瀑布流**：弃用了会导致排版顺序错乱的传统 CSS Multi-column 方案，改为在 Vue 端利用 ResizeObserver 动态计算屏幕宽度并划分为多列（packColumns），在维持瀑布流美观的同时，**完美保留了卡片从左到右、从上到下的自然阅读顺序**。
* **消除展开偏移影响**：得益于上述 Vue 分列计算逻辑，某一个作物包展开内部指标时，**只会使其正下方的卡片向下移动**，彻底解决了之前“一处展开、全屏卡片乱跳”的痛点。

### 4. 权限矩阵显示 (Permission Matrix)
* **去 Emoji 化，全面引入 Material 规范**：权限矩阵表格去除了之前生硬的纯文本 Emoji（如 🟢、🚫）。引入了全局 ormatPerm 机制，将权限点统一替换为了规范的 **Google Material Symbols**（如 check_circle、lock 等），视觉风格更严谨专业。

### 5. 顶栏农场选择器 (Top bar Farm Selector)
* **自定义 Vue 下拉组件**：为了解决系统原生 <select> 在不同浏览器下样式不一致、带有难看阴影和蓝色高亮边框的问题，我们抛弃了原生控件，重写了一套纯 Vue 驱动的自定义下拉菜单（Custom Dropdown）。
* **UI 风格统一**：现在的农场选择器居中显示，宽度自适应文本，背景与头部导航条完美融合，不再有违和的 focus outline 和错位遮挡问题。

### 6. 系统管理员头像与个人中心弹窗 (Avatar & Popup)
* **动态 Material 头像**：将系统管理员右上角的默认头像（⚙️）改为了更契合系统的 Google Material Symbols（dmin_panel_settings）。
* **“点击外部关闭”机制重构 (Click-outside)**：修复了个人中心弹窗在点击其他空白区域时无法自动关闭的 Bug。从依赖 #app 的普通点击冒泡，升级为了**全局捕获阶段 (Capture Phase) 事件监听**。现在无论点击页面何处（哪怕是带有 @click.stop 的其他按钮），弹窗都会灵敏且正确地关闭。

---

## 二、 后端相关调整
* **AdminManagementService 与 AgriApplication**：扩展了系统管理员的账户创建和信息修改能力。
* **CropPackCatalog**：修复了作物包状态同步及查询时的空指针或权限相关编译问题。
* **离线容错 (Offline Fallback)**：解决了后端服务离线时，管理员登录页面的回退逻辑及运行监控历史记录渲染失败的问题。

**附注**：以上所有改动均已在 SysAdminUI 分支本地经过充分验证。建议队长在合并前拉取最新 SysAdminUI 并在本地运行检阅。
