# 农户端地块稳定排序与拖拽排序验收

> 最近验收日期：2026-09-03
> 功能编号：T-134（排序基线）/ T-152（拖动可用性修复）/ T-153（V5.9.21 键盘排序移除）
> 范围：本地 `main` 预览工作树；V5.9.21 未推送、未部署。既有排序后端基线曾随 `165aefd9` 发布，本轮不修改接口、权限或服务器。

## 功能证据

- 后端 `/api/v1/plots` 按 `plotId` 排序，新增 `GET/PUT /api/v1/users/me/preferences/farmer-workspace`，配置按农户账号隔离并通过数据库 `entity_record` 持久化。
- PUT 使用 `expectedRevision`；版本不一致返回 `409 FARMER_WORKSPACE_PREFERENCE_CONFLICT`。请求只接受数组，地块权限、停用地块和数量上限在服务端校验。
- 当前可见地块按“已保存顺序 → 新/恢复地块按 `plotId` 追加”合并；遥测和工作区刷新只更新卡片内容，不重排。
- 农户首页、我的地块、地块下拉选择和工具入口均消费同一个顺序状态。
- “我的地块”使用 Pointer Events：桌面可按住右上角六点手柄立即拖动，触屏仍可长按卡片约 400ms 激活，激活前移动超过 8px 会保留页面滚动并取消长按；拖动中页面和其他地块保持可见，完整卡片跟随指针，目标卡高亮并显示“放到「地块名」的位置”。
- 排序手柄只用于指针拖动，不进入键盘焦点序列，也不绑定方向键、Home 或 End；鼠标或触屏每次换位只保存一次，指针取消、版本冲突和保存失败均回滚。
- 农户与管理员复用十一项指标常量：土壤湿度、空气温度、空气湿度、光照、二氧化碳、降雨量、酸碱度、水位、速效氮、速效磷、速效钾；缺失值显示 `—`，未知扩展指标按编码稳定追加。

## 集中验证

| 验证项 | 结果 |
| --- | --- |
| Web 全量测试 | `npm test -- --test-concurrency=1`：167/167 |
| 农户排序定向测试 | `farmer-plot-drag`、`plot-display`、`api-session`：28/28 |
| 脚本语法 | `farmer.js`、`icon-map.js`、`farmer-plot-drag.test.js`：通过 |
| 生产构建 | Vite：67 modules，通过 |
| API/OpenAPI | 本轮合同未变，未重复执行后端测试 |
| 差异检查 | `git diff --check`：通过 |

## 浏览器验收

页面：`http://127.0.0.1:3001/farmer.html#plots`

- 初始卡片顺序为 `plot-a01 → plot-a02`；对“温室1”手柄发送右方向键后顺序保持不变，手柄 `tabindex="-1"`，提示只保留“拖动调整顺序”。
- 按住第一张卡的六点手柄直接拖到第二张卡后变为 `plot-a02 → plot-a01`，页面显示“地块排列已保存”；刷新后顺序仍保留。
- 再次使用鼠标手柄拖动可恢复 `plot-a01 → plot-a02`，最终刷新仍保持恢复后的顺序。
- 手柄实际使用 `ph-dots-six-vertical` 图标且处于可拖动状态；707px 宽视口下 `scrollWidth === clientWidth`，无页面横向溢出。
- 普通地块详情语义、十一项指标顺序和后端偏好合同保持不变。

本次未对远程服务器执行部署或线上写入。
