# AgriLoop 交接文档：资源分配与风险推演重构（2026-08-28）

## 1. 当前基线与工作区状态

- 项目目录：`D:\26年暑期实训项目\smart-agriculture-main-local`
- 分支：本地 `main`
- 基线提交：`a10c88b feat: add AI water allocation and automated irrigation`
- 当前工作区有未提交的本地增量；尚未推送 GitHub，尚未部署服务器。
- 本轮只使用本地模拟/演示数据，没有操作真实 BearPi。

## 2. 今天已完成的代码改动

### 后端只读推演

- 在 `apps/api-service/src/main/java/com/agriloop/AgriApplication.java` 新增 `forecastEvaluate`。
- `POST /api/v1/forecasts/evaluate` 读取 `plotId`、`metric`、`scenario`、`parameters`、`requestVersion`，合并当前地块策略并执行同一套后端确定性模型。
- 试算使用 `persist=false`，不会保存 `forecast`、修改策略、发布事件或同步模拟器配置。
- 响应增加 `persisted=false`、`requestVersion`、`modelMode`、`dataSource`、`inputSnapshot`、`explanation`、`warnings`。
- 保留后端参数裁剪和干旱/积水阈值校验。

### 管理员资源安排页

- 重写 `apps/web-ui/js/modules/admin-resource-planning.js`：
  - 配额、实际用量、预留量、余额 KPI；
  - 地块风险/需求优先队列；
  - 实测、AI 推演、置信区间图表和指标选择；
  - 计划表格展示申请、分配、缺口、设备、时段、状态；
  - 调整方案改为弹窗表单并强制填写原因；
  - 保留 AI 分析、未来配额、确认、取消、人工兜底和账本统计。
- 重写 `apps/web-ui/css/modules/admin-resource-planning.css`：使用 `--g-*` 变量，去除深色水舱装饰、伪水池、压缩文本和无效留白，增加窄屏布局。

### 地块详情实时推演

- 修改 `apps/web-ui/js/app.js`：删除 `localPreviewCurve`，场景、滑块或指标变化后约 300ms 防抖调用 `api.evaluateRiskForecast`。
- 请求使用序号丢弃乱序响应；请求期间保留上一条曲线并降低预测线强调度；失败或数据不足时不绘制假曲线。
- 只有“保存到此地块”才修改正式策略。
- 修改 `apps/web-ui/js/api.js`：新增 `evaluateRiskForecast`；在线模式 POST 后端，演示模式通过 API 模拟层返回确定性、明确标记为模拟的数据。

### 独立风险入口（已完成一部分）

- 从 `apps/web-ui/js/roles.js` 管理员和农户视图权限移除 `risk-forecast`。
- 从 `apps/web-ui/js/app.js` 主导航和组件注册移除独立风险入口。
- 删除 `apps/web-ui/index.html` 中 `tmpl-risk-forecast` 模板。
- 旧管理员入口暂时映射回 `dashboard`。

## 3. 尚未完成、必须继续完成

### P0：入口清理与运行时确认

1. `apps/web-ui/js/farmer.js` 仍保留 `tools/risk` 分页、风险曲线和情景逻辑。需要将旧 `#tools/risk` 跳回 `#dashboard` 或地块详情，工具台只保留作物培养手册，并清理相关解析、导航、模板和 CSS。
2. `apps/web-ui/js/app.js` 中的 `RiskForecastView` 定义仍是死代码；确认路由无引用后删除。
3. 检查资源页 Vue 模板、ECharts 初始化、watch 和空数据兼容，确保演示/在线模式不会出现 NaN、空白或控制台错误。
4. 搜索 `apps/web-ui/css/modules/work-orders.css` 中遗留的资源选择器，删除或确认未加载，避免重新覆盖专用资源页样式。

### P1：测试、接口文档和状态记录

- 后端测试：只读试算不落库、参数/场景改变曲线、首点等于最新遥测、版本回显、阈值错误、权限隔离和预测不可用。
- 前端测试：300ms 防抖、乱序响应忽略、保存前不回写、指标切换、错误不画假曲线、旧风险路由跳转、资源计划按钮和调整弹窗。
- 更新 `docs/api/openapi.yaml`、`PROJECT_STATUS.md`、`TASKS.md`，写明新试算合同和风险页面归并状态。

### P1：本地构建与浏览器验收

执行：

```powershell
cd D:\26年暑期实训项目\smart-agriculture-main-local\apps\web-ui
npm test
npx vite build

cd ..\api-service
..\gradlew :apps:api-service:test --no-daemon
..\gradlew :apps:api-service:bootJar --no-daemon
```

再执行：

```powershell
cd D:\26年暑期实训项目\smart-agriculture-main-local
git diff --check
git status --short
```

浏览器至少验收：管理员资源安排、管理员总览地块详情、农户旧风险地址跳转、1440×1000 和窄屏、明暗主题、无控制台错误、无重复 4xx/5xx、不可用预测不显示假曲线。

## 4. 已知风险与注意事项

- 当前没有执行服务器构建，不能直接部署这批未验证代码。
- Java 后端和 JS 演示 API 各自维护模型实现，必须保持响应字段、边界和算法版本一致；数值计算不能重新放回 Vue 组件。
- 本轮没有修改真实设备、MQTT ACK、自动灌溉执行、资源计划确认或人工兜底核心逻辑；不要扩大到真实 BearPi。
- 完成 P0/P1 后再使用约定身份创建本地提交：`Banana2400 <483971956@qq.com>`；默认不推送 GitHub。

## 5. 接手建议顺序

1. 清理农户端风险入口和 `RiskForecastView` 死代码。
2. 运行前端语法检查、Node 回归、Vite 构建，修复资源页运行时问题。
3. 补齐后端/前端测试及 OpenAPI、状态文档。
4. 启动本地 API 与 Web，按验收清单截图留证。
5. 检查 diff、创建本地提交，再另行决定服务器部署。

