# 智慧农业项目进度

> 项目：农智闭环（AgriLoop）
> 更新时间：2026-09-02
> 2026-09-02 补光时段显示系统时间（本地实现，待用户验收）：虚拟补光确认面板的选项改为显示本机系统时间起止范围，后台仍以 60/300/600/900 秒传递和记录；执行仍为即时虚拟演示，不等待真实时长。
> 2026-09-02 光照补光时长选择（本地实现，待用户验收）：虚拟补光确认面板新增 1/5/10/15 分钟时长选项，预览、命令参数和执行记录统一使用所选时长；后端安全上限仍为 900 秒，动作继续标记为 SIMULATED。
> 2026-09-02 农户按钮/卡片交互优化（本地实现，待用户验收）：统一农户端按钮与可点击卡片的悬停、按压、键盘焦点和禁用反馈，补充风险卡、地块卡、统计卡、操作系统页签和工具页签的触控反馈；不改变业务逻辑、权限或数据来源。前端 144/144、JS 语法、Vite 构建、`git diff --check` 与本地浏览器视觉复核通过。
> 2026-09-02 main 最新发布：本地 `main` 在保留远端最新 `main@1d67dda`（设置/布局、AI 多选和 CI 修复）的基础上合并 AI 直答改动，最终提交 `dfc1dbe0e4d2b8ccf0d1f356a79b04e3d4af59ee` 已推送 GitHub `main`。同一源码已部署到 `/srv/agriloop/app`，`DEPLOYED_COMMIT`、`deployed-main.txt` 与 `.deployed-commit` 均一致；API、Qwen 模型、Supervisor 和三入口静态资源核验通过，登录后 Agent 返回 `adapter=openai-compatible`、`degraded=false`。服务器保留数据库备份 `/srv/agriloop/backups/agri-20260902-121206.sql.gz` 与源码回滚目录 `/srv/agriloop/releases/pre-dfc1dbe-20260902-120825-backup`，未修改其他角色副本目录。
> 2026-09-02 V5.9.11 全界面中文化收口（T-143，本地验收，待用户验收）：登录页、备用登录页、农场管理员、系统管理员、种植农户工作台和原生微信小程序的系统文案统一为简体中文，保留 `agriloop` logo、标准单位、版本、网址、技术编号与用户原始内容。公共展示层补齐角色、状态、来源、服务、事件和错误码映射，未知英文枚举使用中文兜底；英文后端错误在前端转为中文并保留原始载荷调试信息；助手系统提示默认使用简体中文。字体选择控件已移除，旧字体偏好自动迁移为系统默认；按用户最终确认，主登录页作为视觉例外保留原仿宋字体，备用登录页、三角色工作台和小程序继续使用系统默认字体。Web Node 148/148、Vite、小程序 6 页面/11 个脚本静态校验、改动脚本语法和差异检查通过；集中浏览器检查覆盖两套登录页与三角色页面且无横向溢出或运行错误。Java 测试被本机 `Unable to establish loopback connection` 环境故障阻断，微信开发者工具未安装，均不虚报为通过；内容已随 `main@dfc1dbe` 推送并部署，线上入口已核验。
> 2026-09-02 消息中心未读状态修复（T-144，已在 farmer-ui 本地验收，未部署线上）：农户端消息列表对未读消息显示红色圆点，点击单条消息详情（包括首页最近动态入口）立即静默改为已读；详情页移除手动“标记已读”按钮，进入消息中心不批量清除其他消息。已读 ID 按正式/演示模式和账号保存在浏览器，刷新可恢复，损坏或不可写时不阻塞页面；消息业务状态、告警/任务后端记录和 OpenAPI 合同不变。定向 Node 13/13、语法检查、Vite 62 modules、桌面/390px 本地浏览器功能与视觉验收通过。
> 2026-09-02 农户灌溉入口、水量校验与布局修复（T-117 增量，本地验收，待用户验收，未发布线上）：人工浇灌按钮在有当前处方的地块上始终显示，正常与 `NO_ACTION` 场景都可打开；打开/提交时仍校验处方、权限、单次/每日水量、当前农场资源余额、最大时长和幂等键。正式 readiness、处方和命令统一按地块所属农场读取水量档案，已完成历史成功命令不再重复计入容量；演示成功执行同步扣减顶部水量余额。灌溉建议区域移除重复的大湿度图并改为单列内容，保留卡内紧凑趋势。输入为当前地块处方、资源余额和操作权限，处理为统一资源限额校验，输出为可见入口、一致反馈和最新余额；缺失处方、权限不足或资源/时长上限不足仍不执行。Web Node 34/34、定向 Java 3/3、`compileJava/compileTestJava`、JS 语法检查、Vite 62 modules、桌面/390px 本地浏览器功能与视觉验收和 `git diff --check` 通过；中文路径 Gradle Test worker 的既有 `ClassNotFoundException` 不作为断言结果，未推送、未部署。
> 2026-09-02 光照子系统建议与智能诊断入口（本地实现，待用户验收）：农户操作系统的光照界面现与灌溉保持同一操作闭环，提供“查看建议并执行”和“查看智能诊断”入口。白天光照不足且满足安全门时进入虚拟补光确认；夜间、正常或高光状态展示规则建议、支持/反对/缺失证据，不生成越界命令。补光仍仅走 SIMULATION_ONLY 虚拟执行器，未接入真实灯具。
> 2026-09-02 光照风险卡片可读性修复（本地实现，待用户验收）：农户操作系统的光照/灌溉地块卡片已限制长标题与目标文本溢出，并通过明显语义边框、左侧强调色、背景、阴影和 `aria-pressed` 明确选中状态，不显示额外文字标识；CSS/HTML 缓存版本已刷新。前端 143/143、Vite 构建及本地浏览器复核通过。
> 2026-09-02 动态光照目标（本地实现，待用户验收）：Crop Pack 阶段目标现按 `lightSchedule` 在白天/夜间动态切换；默认 06:00—18:00 为白天生长目标，夜间为 0~1000 lux 休息带。夜间不触发缺光告警、不开放虚拟补光；高光告警、曲线昼夜目标带、健康评分和后端虚拟执行均已同步。前端 143/143、Vite、Crop Pack 校验和后端两项动态光照定向测试通过；完整 Gradle 套件另有既有 H2 场景连接失败，待环境稳定后重跑。
> 2026-09-02 农户操作系统子系统与脚本缓存修复（T-142，本地验收，待用户验收）：农户“操作系统”提供“灌溉系统 / 光照系统”切换；灌溉保留土壤湿度曲线、灌水预警和补水执行，光照展示强度曲线、目标带、低/高告警与虚拟补光。三入口及聊天模块缓存版本已刷新，避免旧缓存继续显示旧来源；本地浏览器确认桌面/390px 页面无横向溢出、控制台无错误。正式补光硬件不在本期范围。
> 当前硬件口径：BearPi E53_IA1 的串口 -> MQTT -> API -> SSE 真实遥测链路已经接入并有实测记录（`sourceMode=REAL`、`provenance=OBSERVED`、`dataOrigin=HARDWARE`）。模拟器仍负责未接入指标和情景演示；灌溉执行仍是虚拟执行。下面较早的验收条目按记录当日范围保留，当前结论以本条和最新状态为准。
> 2026-09-02 文档口径同步：README、功能/技术架构、路线、MQTT 合同、任务与验收说明已统一反映上述混合数据链路；本轮仅更新描述，不改变接口、权限或执行逻辑。
> 2026-09-02 三角色工作台设置视觉统一（T-141，本地实现，待验收）：三个入口共用的工作台设置现在统一采用农场管理员参考的紧凑卡片导航与经典材质，系统管理员和农户的侧栏同样显示彩色语义图标、卡片背景、边框、阴影及一致的悬停/选中状态；设置页卡片和风格选项保持同一外观。左下角设置入口、角色权限和业务逻辑未改；共享 CSS 查询版本已刷新。本轮未运行完整测试套件。
> 2026-09-02 原生微信小程序端（T-140，本地实现，待开发者工具验收）：新增不依赖 `web-view` 的原生 WXML/WXSS/JavaScript 小程序，包含三角色登录、角色化总览、地块多指标/预测、任务与告警、按账号/地块隔离的 AI 会话、原图直传和受控操作确认。接口复用现有 `/api/v1`，服务端继续负责角色、农场、地块和会话权限；服务异常时显示规则降级，不伪造模型或实时数据。`node scripts/check-wechat-mini-program.mjs`、各 JS `node --check`、JSON 解析、WXML 事件绑定和 `git diff --check` 已通过。正式 AppID、HTTPS 合法域名、微信开发者工具真机验收尚未配置，本轮未推送 GitHub 或部署服务器。详见 [`apps/wechat-mini-program/README.md`](apps/wechat-mini-program/README.md)。
> 2026-09-02 手机端兼容（T-139，已推送并部署）：三角色工作台补齐 760px 以下的导航抽屉、遮罩与 `Escape` 关闭行为，导航切换后自动收起；共享内容区、表格、任务汇总和长标识增加宽度约束与触摸横向滚动，320px 头部/仿真控制栏/农户培养手册标签适配窄屏。系统管理员、农场管理员和农户的 320px/390px 关键路由均无全局横向溢出，`node --check`、`git diff --check` 与 `npx vite build` 通过。提交 `58989698e75ac213ade76f9242f51689b5209f87` 已推送 `main` 并发布到 `/srv/agriloop`，Nginx 配置重载、API 健康、Supervisor 和三入口 HTTP 200 均通过；回滚备份为 `/srv/agriloop/releases/pre-5898969-20260902-062151-backup`，其他角色副本未改动。
> 2026-09-02 受控学习质量门（T-138，已推送并部署，待用户验收）：在既有决策/告警案例、反馈、执行 ACK、效果评价、规则集和 Crop Pack 上增量加入 `PENDING/QUALIFIED/REJECTED` 确定性质量判断、正负学习用途隔离、冻结来源快照、会话/账号/农场/地块权限过滤，以及 `DRAFT -> OFFLINE_VALIDATED -> APPROVED -> ACTIVE -> ROLLED_BACK` 候选状态机。失败、漂移、冲突、过期、无 ACK、不可评价或未确认模拟案例只保留为反例和审计记录；不在线修改模型权重、生产规则或设备控制逻辑。治理界面展示判定依据、用途、来源和引用案例，详见 [`docs/controlled-learning.md`](docs/controlled-learning.md)。随 `main@dfc1dbe` 部署，未运行完整测试套件。
> 2026-09-02 AI 直答模式（T-142，已推送并部署，待用户验收）：OpenAI-compatible 对话请求固定关闭 Qwen 思考和推理强度参数，并拒绝把 `reasoning_content` 当作用户答复；模型可用时普通问题、问候、模糊短句和能力问答均由模型自然生成，不再以规则快捷答复替代模型。规则只用于空输入/编号澄清、受控写操作安全门和依赖故障降级；演示来源明确标为未连接模型。兼容 `full` 模式别名，远程配置脚本固定 `AI_MODE=openai-compatible`、`LLM_ENABLE_THINKING=false`、`LLM_REASONING_EFFORT=none`。`main@dfc1dbe` 已推送并部署到 `/srv/agriloop`，健康接口、模型接口、三入口和登录后 Agent 直答核验通过；本轮未运行完整测试套件。
> 2026-09-02 CI 与首屏性能收口（已随 main 推送并部署）：兼容旧 `/strategy-candidates` 手工候选时仍强制执行确定性离线回放和人工批准，但标记 `MANUAL_AUTHORED`、`learningEligible=false`、`learningUses=[NONE]`，不会伪装成正向学习案例。农场管理员、系统管理员和种植农户工作台改为核心数据先绘制、治理/历史/助手数据后台补齐；每个核心请求有约 2.2 秒预算，超时保留可用页面并由轮询重试，SSE 不再阻塞路由。三入口脚本缓存版本同步刷新，避免远端浏览器继续使用旧加载逻辑。GitHub 失败用例已在 ASCII 路径复核通过，线上三入口和 API 健康检查通过。
> 2026-09-01 V5.9.10 自注册农场与地块接入（本地验收，待用户验收）：自注册 FARM_ADMIN 必须填写农场名称和地区，服务端在同一事务内创建账号及独立空农场，初始地块范围为空，不再继承 `farm-demo` 的地块、设备、遥测或任务；任一写入失败整体回滚并返回 `503 ACCOUNT_PERSISTENCE_UNAVAILABLE`。空农场总览提供可稍后跳过的“添加第一块地”正常引导，复用现有表单录入作物、品种、阶段、设施、生长周期、面积及可选设备；地块仅在持久化成功后返回，数据库不可用返回 `503 PLOT_PERSISTENCE_UNAVAILABLE`。FARM_ADMIN 地块权限按授权农场动态判定，创建前签发的 JWT 可立即读取和修改本场新地块，跨农场访问仍为 403。隔离真实 HTTP 使用两个新管理员验证农场与业务数据完全隔离，创建及修改地块后重启文件型 H2，原农场 ID、地块 ID 与修改内容仍可查询。Web Node 124/124、Java JUnit 89/89（主源码/测试直接编译）、Vite、JS、OpenAPI YAML 与差异检查通过；正式注册页、空农场和首块地闭环在桌面与 698px 均无横向溢出，页面无 warning/error。完整 Gradle 仍在编译前被本机 Java NIO loopback 故障阻断，未虚报为通过；未推送、未部署。详见 `T-137`。
> 2026-09-01 V5.9.9 受控三角色账号创建（历史交付，自注册 FARM_ADMIN 范围已由 T-137 替代）：登录页注册开放种植农户、农场管理员和系统管理员三种身份；FARMER 保持示范农场默认地块，V5.9.9 的 FARM_ADMIN 示范农场范围已在 V5.9.10 改为独立空农场，SYSTEM_ADMIN 必须使用部署环境提供的 `SYSTEM_ADMIN_AUTHORIZATION_CODE`，仓库无默认密钥，授权码采用恒定时间比较且连续失败 5 次后限制 15 分钟。SYSTEM_ADMIN 工作台改为真实全局账号列表和角色化创建表单，非系统管理员账号支持停用、启用和删除，全部 SYSTEM_ADMIN 永久禁止停用或删除，创建成功只显示一次恢复码；`/farm-members` 仅允许本场 FARMER，关闭农场管理员创建高权限账号的绕过路径。账号创建、状态、删除和范围均在持久化成功后返回，数据库不可用统一拒写 `503 ACCOUNT_PERSISTENCE_UNAVAILABLE`。隔离真实 HTTP 已验证三角色注册、错误授权拒绝、全局三角色创建、敏感字段隔离、越权拒绝、账号保护与文件型 H2 重启后同 ID/范围仍在。Web Node 122/122、Java JUnit 77/77（主源码/测试直接编译）、Vite、JS、OpenAPI YAML 和差异检查通过；正式页面确认桌面与 698px 注册/账号管理无横向溢出，系统账号保护按钮禁用，控制台无 warning/error。完整 Gradle 仍在守护进程启动前被本机 Java NIO loopback 故障阻断，未虚报为通过；未推送、未部署。详见 `T-136`。
> 当前功能发布：`main` 已整合最新 `SysAdminUI`、`farmer-ui` 与管理员增量；资源安排已升级为配额、风险、需求、分配与用水事实同页核对，农户端在“更多工具”提供负责地块的参数化只读风险双轨试算、完整作物培养手册和农智助手。三角色共享工作台设置保留左下角入口、白色默认主题、黑色/跟随系统主题、经典卡片/液态玻璃、强调色、密度、宽度、动效、自动刷新和来源标识；工作台固定继承系统默认字体，旧字体偏好会自动归一，主登录页按用户要求保留原仿宋字体。柔和玻璃与地块图片偏好已退出，旧浏览器配置自动迁移为经典卡片和纯色地块，所有外观偏好即时静默生效。
> 2026-09-02 灌溉建议安全门简化与补证闭环修复（本地实现，自动化与构建通过，未发布线上）：readiness 统一增加 `blockingEvidence`、`advisoryEvidence`、`executionAllowed`；仅在新鲜 GOOD 遥测、设备在线、无高置信漂移/故障、非暴雨、资源/权限/时长通过、湿度不低于 Crop Pack 自动浇水阈值且为 `OPERATOR_CONFIRMED` 的常规人工灌溉中，把 `MORE_DIAGNOSIS_EVIDENCE`/`HUMAN_EVIDENCE_REVIEW` 降为提醒，证据和冲突历史仍保留。自动/应急安全门、坏数据、过期遥测、漂移、离线、暴雨、资源/权限/时长失败继续硬阻断；补证按地块+证据类型+未完成工单去重，复测匹配标记旧冲突 `RESOLVED`，新冲突标记旧冲突 `SUPERSEDED`，普通巡田无便携仪值不伪造复测；巡田保存后只执行一次处方→readiness→诊断刷新链路。后端 `compileJava/compileTestJava`、ASCII 驱动映射定向 Java 3/3、前端定向 Node 17/17、JS 检查、Vite 构建、OpenAPI 校验和差异检查已通过；中文路径的 Gradle Test worker 仍报告测试类 `ClassNotFoundException`，未把它当作断言通过。线上页面只读加载尝试超时，未登录、未执行虚拟浇水或部署，仅验证本地模拟数据和虚拟执行。
> GitHub 同步：本地已合并最新 `origin/main@bd3e578`，并以 `Banana2400 <483971956@qq.com>` 将发布提交 `5511efe` 推送到 GitHub `main`。相同 Web 快照已差异发布到 `/srv/farm-admin` 和中央 `/srv/agriloop`，其他角色副本目录未单独改动。
> 2026-09-01 main 合并与服务器发布：`farmer-ui` 已合入最新 GitHub `main`，发布提交 `165aefd9da72fc7f98eafe71eea5e89fe741e724` 已同步至 `/srv/agriloop`；服务器端源码包编译、API 健康检查、Supervisor 状态和公网静态入口均通过。数据库备份为 `/srv/agriloop/backups/agri-165aefd9-20260901-194649.sql.gz`，旧应用回滚目录为 `/srv/agriloop/releases/pre-165aefd9-20260901-195301-backup`。本次只完成 HTTP/服务级发布核验，登录后的双角色浏览器操作仍待集中验收。
> 2026-09-01 巡田记录与补证申请双角色可见修复（本地实现与自动化验收完成，浏览器双角色验收待补，待服务器部署）：`GET /api/v1/inspections` 支持农场/地块筛选并按角色裁剪，农户只看本人提交或分配给本人的记录，农场管理员查看授权当前农场全部记录；巡田记录与 `READINESS` 补证申请均在数据库持久化成功后返回并发布 SSE，失败返回 `503 OPERATION_RECORD_PERSISTENCE_UNAVAILABLE`。农户提交后立即显示服务端记录并只刷新一次，照片上传失败单独提示；农场管理员在现有“农务任务”页增加独立补证申请区和最新 8 条巡田记录展开区，读取失败保留旧数据并提示。Web Node 118/118、Java 直接 JUnit 81/81、Vite、OpenAPI 校验和静态检查通过；Gradle 测试任务在当前 Windows 路径下的 worker `@classpath` 无法加载测试类，非断言失败，详见验收记录。本轮浏览器已检查农户记录提交/刷新和管理员巡田展示，但切换演示身份时被本地 URL 安全策略中断，未将完整双角色补证申请浏览器检查写成通过。线上服务器尚未部署。详见 `T-135` 与 `docs/acceptance/INSPECTION_VISIBILITY_ACCEPTANCE.md`。
> 2026-09-01 农户端地块稳定排序与拖拽排序（本地验收完成，待服务器部署）：`/api/v1/plots` 改为按 `plotId` 稳定排序；新增按农户账号隔离、数据库持久化的 `farmer-workspace` 地块顺序偏好，PUT 通过 `expectedRevision` 防止多设备静默覆盖，并按权限、停用、新增规则合并地块。农户首页、我的地块、下拉选择和工具入口统一消费该顺序；“我的地块”使用 Pointer Events 支持约 400ms 长按拖动、8px 误触取消、Escape/指针取消/保存失败回滚和一次性提交。农户与管理员共用十一项标准指标顺序，缺失值显示 `—`，扩展指标稳定追加。Web 114/114、Java Gradle 79/79、Vite、OpenAPI YAML、桌面/390px 浏览器功能视觉验收和控制台检查通过；本地提交后推送 GitHub `farmer-ui`，线上服务器尚未部署。详见 `T-134` 与 `docs/acceptance/FARMER_PLOT_ORDER_ACCEPTANCE.md`。
> 2026-09-01 农户任务刷新与问题上报闭环（本地修复，待服务器部署/用户验收）：农户执行开始或提交结果后，前端按工单编号重新绑定弹窗、流程和任务列表引用，正式接口成功后刷新一次工作台，避免旧状态继续显示“执行”。“上报问题”改为要求填写具体描述和优先级，后端校验权限、长度、任务状态和重复上报，写入与原任务关联的 `FARMER_REPORT` 工单并发布单条 `workorder.farmer-report` 事件；农场管理员通过总览计数、农务任务筛选、动态和实时提示接收，农户侧不重复展示报告工单。失败时保留表单错误和后端错误码，正式服务不可用仍沿用现有降级提示，不伪造管理员已收到。Web 定向 26/26（完整前端 109/109）、Java Gradle 全量、Vite、差异检查和一次本地浏览器关键路径验收通过；服务器尚未部署，需部署后做一次线上验收。
> 2026-09-01 农户灌溉人工证据冲突前置阻断（本地修复，待服务器部署/用户验收）：线上“名称 Agent验收临时地块”存在人工现场证据与遥测冲突，后端安全门已正确返回 `HUMAN_REVIEW` 和 `HUMAN_EVIDENCE_REVIEW`，但旧农户前端确认页漏判 `HUMAN_REVIEW`，导致点击“确认并执行”后才弹出阻断错误。已在 `farmer.js` 前置阻断 `HUMAN_REVIEW`、补齐人工证据中文映射，并在 API 同步映射错误码；不会放宽安全门，用户应转巡田/复测或人工复核。定向 Web Node 1/1、API `compileJava`、Vite、差异检查和本地确认弹窗/控制台检查通过；线上静态脚本仍为旧版本，需部署本次提交后再做一次线上验收。
> 2026-09-01 设备离线恢复修复（已部署，待用户验收）：模拟设备在“设备离线”场景下成功执行管理员开启后持久化手动在线覆盖，后续模拟状态、遥测设备读模型和过期检查不再覆盖为离线；模拟器现在按地块实际绑定的模拟设备编号（包括自定义编号）写入遥测和状态，再次关闭、解绑或换绑会清除覆盖。新增非 `mock-` 设备控制回归用例；Java 生产/测试代码编译通过，未运行完整测试套件。远端 `/srv/agriloop` 提交标记为 `bbab0805e7e78cb48c9402e1c4b79bdd08b43402`，健康检查 `UP`；`002` 跨“设备离线”场景模拟周期保持在线且遥测持续更新。
> 2026-09-01 V5.9.8 国内行情数据补齐（本地验收，待用户验收）：农业农村部公开报价接口核验确认重庆当日仅覆盖番茄、黄瓜、茄子、生菜和辣椒；当前公开图表接口忽略已核验的历史日期参数，历史列表又受验证码保护，因此未将其假定为稳定回填接口。正式行情现改为“重庆优先、全国后备”，重庆缺报时才读取同一官方接口的全国上报市场简单均值。2026-09-01 隔离 HTTP 实测九种监测作物全部有官方当日价，其中 5 种为重庆报价、玉米/大米/草莓/葵花籽 4 种为全国后备，卡片和明细逐项标明区域与口径。真实归档少于 3 日时，前端默认显示以当日官方价为锚点的确定性紫色虚线“模拟趋势”，同时提供隐藏开关和 `SIMULATED` 说明；模拟序列不写数据库、不进入真实历史表、涨跌、7 日统计或销售观察。首次九品种 HTTP 刷新 962ms，缓存后 21–50ms；Web Node 119/119、国内/全球定向 13/13、Java 行情测试 6/6（主源码/测试直接编译）、Vite、JS 和差异检查通过。正式 FARM_ADMIN 页面确认 9/9、全国后备 4 种、模拟趋势开关、伦敦 DEFRA 隔离及 698px 无横向溢出，页面日志无 warning/error。完整 Gradle 仍被本机 Java NIO loopback 故障阻断，未虚报为通过；未推送、未部署。详见 `T-131`。
> 2026-09-01 V5.9.7 国内行情本地化收口（本地验收，待用户验收）：FARM_ADMIN“国内行情”固定只展示重庆真实日度报价和系统逐日归档，默认近 30 日并保留 7/30/90 日切换；移除重庆/国际数据源切换、6/12/24 期范围、英国 DEFRA 横幅、国际曲线、英镑单位、原始观测表和“有国际参考”缺失提示。归档不足时明确说明不使用外部参考或模拟值补齐；缺失日期断线、同日市场比较、销售观察和 15 分钟刷新合同不变。后端 `internationalReference` 字段、DEFRA 资源及测试继续保留，全球批发伦敦节点仍展示独立 `GBP/kg` 外部观测且不进入到岸成本或毛差。Web Node 117/117、国内/全球行情定向 11/11、Vite 构建、JS 检查和 `git diff --check` 通过；1440px 与 698px 正式管理员页面确认国内无国际参考、三档范围切换正常、全球伦敦引用仍在，两种宽度均无横向溢出或错误面板。未修改后端接口，未推送、未部署。详见 `T-130`。
> 2026-09-01 V5.9.6 官方世界标准地图（本地验收，待用户验收）：全球批发底图改用自然资源部标准地图服务提供的“世界地图（小 8 开、分国设色）”官方原图，完整保留 `GS(2016)1663号` 审图号和“自然资源部 监制”，原始 JPEG 为 4655×2444、536,363 字节，SHA-256 已登记；页面不裁切、不重绘国界，重庆农场、9 个目的地和当前模拟关系线均置于独立 HTML/SVG 业务覆盖层，并明确说明城市位置和关系线不是实际航线、公开发布含覆盖层的地图前仍需履行地图审核。移除 Natural Earth GeoJSON 与 ECharts 地图注册，官方图片本地懒加载且低优先级解码，地图失败时目的地队列、成本测算和明细仍可使用。Web Node 116/116、全球批发定向 5/5、Vite 构建、JS 检查和 `git diff --check` 通过；正式管理员页面确认原图自然尺寸 4655×2444、审图号可见、9 个目的地及路线切换正常，698px 收起导航后无横向溢出。未修改后端接口，未推送、未部署。详见 `T-129`。
> 2026-09-01 V5.9.5 全球批发销售沙盘（本地验收，待用户验收）：在 FARM_ADMIN“市场行情”内新增与国内真实行情严格分区的“全球批发”模式，保留原 `/market-prices`、重庆日价、英国 DEFRA 原币参考及权限合同不变。页面以重庆农场为起点提供东京、新加坡、迪拜、鹿特丹、伦敦、洛杉矶、多伦多、悉尼和圣保罗 9 个城市级批发节点、19 条航空/铁路/海运冷链场景，并按球面距离、运输模式、时效、包装、模拟边境成本和损耗计算到岸成本、可售量、目标销售额与毛差；物流、边境成本和默认买方价全部标记 `SIMULATED`、结果 `executable=false`，正式用户输入单独标记 `USER_PROVIDED`，英国观测仍保留 `GBP/kg` 且不换汇、不进入到岸或毛差。地图采用本地 Natural Earth 1:110m Public Domain 陆地轮廓（138.16 KB，SHA-256 已登记），首次进入才加载，仅绘制当前关系线和最多 9 个节点，无在线瓦片、持续动画或新增地图 SDK；地图失败时目的地列表和明细表仍可用。Web Node 116/116、全球批发/行情定向 10/10、Vite 构建、JS 检查和 `git diff --check` 通过；桌面与 698px 正式管理员页面完成目的地、运输方式、输入联动、DEFRA 分离和降级结构检查，698px 无横向溢出、控制台无 warning/error。未修改后端接口，未推送、未部署。详见 `T-128`。
> 2026-09-01 V5.9.4 国际参考历史曲线（本地验收，待用户验收）：保留重庆真实日价、H2 每日归档和销售观察合同，新增与本地事实严格分离的英国 DEFRA 果蔬批发参考序列。当重庆归档不足 3 日时，番茄、黄瓜、青椒、草莓可默认查看国际参考；共纳入 2025-09-01 至 2026-08-17 的 62 个官方原始观测点，保留英镑/公斤和发布时间，不做汇率换算、日值插值或重庆历史补写，超过一个发布周期的缺口断线。页面提供“重庆归档 / 国际参考”及 6/12/24 期切换、来源链接和原始观测表；国际参考不进入重庆涨跌、7 日均价、市场比较或销售判断。Web Node 111/111、新增后端行情测试 5/5、Java 主源码与测试直接编译、隔离/正式 HTTP、Vite、`git diff --check` 和桌面/698px 浏览器通过；698px 无横向溢出、控制台无 warning/error。完整 Gradle 仍被本机 Java NIO loopback 故障阻断，未虚报为通过；未推送、未部署。详见 `T-127`。
> 2026-09-01 V5.9.3 农场管理员日度市场行情（本地验收，待用户验收）：新增独立“市场行情”入口，仅 FARM_ADMIN 可查看授权农场；默认聚焦本场在种作物，可切换九种 Crop Pack 监测品种。正式模式读取农业农村部全国农产品批发市场价格信息系统的重庆日度市场上报价，优先展示重庆双福国际农贸城，缺失时使用省内上报市场简单平均并明确标注非成交量加权；当日快照写入现有持久化实体库，7/30/90 日曲线随每日真实归档累积，缺失日期断线且不以 Demo 补齐。行情终端同时提供 Emoji 作物卡、涨跌文字与箭头、市场低高边界、7 日均价/变化、可访问日价明细、同日市场比较及确定性“销售观察”，明确不是证券级实时成交或自动卖出信号。2026-09-01 上游实测九种映射中番茄、黄瓜、茄子、生菜、辣椒有重庆报价，其余四种保持“暂无上报”。Web Node 110/110、新增 Java 行情测试 4/4、Java 主源码与测试直接编译、OpenAPI YAML、Vite 构建、`git diff --check` 及桌面/698px 浏览器检查通过；698px 无横向溢出、控制台无 warning/error。完整 Gradle 仍被本机 Java NIO loopback 故障阻断，未虚报为通过；未推送、未部署。详见 `T-126`。
> 2026-09-01 V5.9.2 Crop Pack 作物 Emoji（本地验收，待用户验收）：农场管理员 Crop Pack 九张作物卡片由名称首字改为系统原生 Emoji，玉米 🌽、黄瓜 🥒、茄子 🍆、生菜 🥬、辣椒 🌶️、水稻 🌾、草莓 🍓、向日葵 🌻、番茄 🍅；优先读取 Crop Pack `icon`，缺失时使用统一映射，未知及自建作物使用 🌱 兜底。卡片尺寸、数据、版本、权限和详情交互不变。定向 Node 27/27、完整前端 99/99、`git diff --check`、Vite 构建及九卡浏览器检查通过；本轮整合后发布到 GitHub `main`，未部署。详见 `T-125`。
> 2026-09-01 V5.9.1 灌溉调度命名收敛（本地验收，待用户验收）：农场管理员“农务任务”中的“资源安排”页签改为“灌溉调度”，页面说明、面包屑和工作台标题同步统一；内部 `resources` 页签标识、Hash 路由、资源接口、三角色权限与持久化协同合同均保持不变。定向 Node 7/7、完整前端 99/99、`git diff --check`、Vite 构建和现有正式管理员标签页浏览器检查通过；本轮整合后发布到 GitHub `main`，未部署。详见 `T-124`。
> 2026-09-01 V5.9 三账号真实资源同步（本地验收，待用户验收）：正式登录令牌改为标签页级 `sessionStorage` 并一次性迁移旧会话，使 FARMER、FARM_ADMIN、SYSTEM_ADMIN 可在同一浏览器独立登录；资源需求、配额和计划统一落入文件型 H2，重启后仍可查询。服务端按“系统管理员全平台只读、农场管理员仅授权农场管理、农户仅所属农场且本人地块/申请/分配”裁剪 REST 与 SSE；数据库不可用时统一返回 `503 RESOURCE_PERSISTENCE_UNAVAILABLE`，三端切换为只读降级。真实 HTTP、越权、版本冲突、幂等、三端 SSE 及重启持久化验收通过。Web Node 98/98、Java Gradle 74/74、Vite 构建、静态检查与三正式账号浏览器检查通过；本轮整合后发布到 GitHub `main`，未部署。详见 `T-123`。
> 2026-08-31 V5.8.9 管理员总览紧凑指标布局（本地实现，待用户验收）：保留十一项指标、显示顺序、数值、单位、靶标和状态合同；按地块卡自身宽度自适应，宽卡由 `3-3-2-3` 四行调整为 `4-4-3` 三行并让数值与靶标同排，中等卡使用三列且靶标独立一行，小卡双列、极窄卡单列。同步压缩地块卡、作物身份区、生长事实区和指标小块的内边距与最小高度。完整前端 71/71、`git diff --check`、Vite 构建及 1180px/698px/390px 浏览器检查通过；698px 首卡由 813px 降至 490px，十一项无截断、三种宽度无横向溢出，详情可打开/关闭，控制台无 warning/error。接口、权限、数据与详情合同不变，未部署服务器。详见 `T-122`。
> 2026-08-31 V5.8.8 管理员总览统一数据表面（本地实现，待用户验收）：保持地块卡 DOM、十一项指标顺序、桌面 `3-3-2-3` 与窄屏双列布局不变，将三项地块事实以及正常、告警、缺失状态的全部指标小块统一为同一浅色数据表面；状态继续由图标和文字颜色表达。普通卡片、作物区、事实区与指标区改用 1px 分层边界，仅顶部地块状态条保留 4px 强调，并移除内部小块阴影。完整前端 71/71、`git diff --check`、Vite 构建及 1440px/698px 浏览器检查通过；77 个指标块与 21 个事实块计算背景色一致，两种宽度均无横向溢出，详情可打开/关闭，控制台无 warning/error，未部署服务器。详见 `T-121`。
> 2026-08-31 V5.8.7 管理员总览边界强化（本地实现，待用户验收）：保持地块标题、作物身份、三项地块事实、十一项指标的顺序与 `3-3-2-3` 布局不变，仅增强地块外框、标题区分隔线、作物身份框、地块事实框和指标网格的边框对比度、粗细与轻量阴影；异常、停用地块继续沿用原状态色，不修改接口、权限或详情交互。完整前端 71/71、`git diff --check`、Vite 构建及 1440px/698px 浏览器检查通过；两种宽度均无横向溢出，详情可打开/关闭，控制台无 warning/error，未部署服务器。详见 `T-120`。
> 2026-08-31 V5.8.6 管理员总览十一项环境与肥力指标卡（本地实现，待用户验收）：依据服务器当前实际遥测编码与 Farmer 共享文案，将地块卡统一为土壤湿度、空气温度、空气湿度、光照、二氧化碳、降雨量、酸碱度、水位、速效氮、速效磷和速效钾十一项；移除服务器当前没有数据的土壤电导率与氮磷钾比占位。桌面端采用 `3-3-2-3` 分行，698px 窄屏为两列并让末项通栏，缺失正式数据仍按原合同显示 `—`。演示地块补齐十一项数据；接口、权限、详情与指标质量逻辑不变。专项 Node 22/22、完整前端 71/71、Vite 构建、1440px/698px 浏览器与控制台检查通过；未部署服务器。详见 `T-090`。
> 2026-08-31 管理员总览作物身份卡（本地实现并完成服务器静态恢复，待用户验收）：移除地块卡整张作物照片和深绿遮罩，恢复浅色数据表面；新增独立作物方框，展示作物名称、品种、阶段事实及原生 Emoji。番茄、玉米、黄瓜、水稻、向日葵、草莓和辣椒使用与农户、系统管理员一致的稳定映射，未知作物使用幼苗 `🌱` 兜底，不猜测作物类型。接口、指标、权限和地块详情交互不变。详见 `T-089`。
> 2026-08-30 管理员告警与 AI 助手入口拆分（本地+farm-admin 分支静态验收）：农场管理员左侧导航新增独立“AI助手”，原“AI告警分析与智能处理”改为“告警智能处理”，告警页移除内嵌对话切换并保持待审核操作、已下发/已关闭只读。AI 助手新增按账号隔离的历史会话栏；正式模式读取 `/api/v1/agent/conversations` 与 `/api/v1/agent/history`，演示模式使用账号级本地持久化。回答按“已知事实—分析判断—执行建议”分层，事实只投影 Agent 返回的工具/结果/诊断数据，规则降级明确标记。旧 `decision-console` chat/assistant 地址自动跳转到 `ai-assistant` 并保留农场、地块上下文；确认/取消和全平台事实域刷新机制保持不变。服务器仅在 `/srv/farm-admin/app/apps/web-ui` 同步提交 `c21ef4c38dc7352d51fc8f43bf7de3f4f55a2331`，公网入口为 `/farm-admin/`；`/srv/agriloop` 总分支已恢复原静态版本且后续不再操作。本轮未推送 GitHub。
> 当前功能发布：`main` 已整合最新 `SysAdminUI`、`farmer-ui` 与管理员增量；“灌溉调度”已升级为配额、风险、需求、分配与用水事实同页核对，农户端在“更多工具”提供负责地块的参数化只读风险双轨试算、完整作物培养手册和农智助手。三角色共享工作台设置保留左下角入口、白色默认主题、黑色/跟随系统主题、经典卡片/液态玻璃、强调色、字体、密度、宽度、动效、自动刷新和来源标识；柔和玻璃与地块图片偏好已退出，旧浏览器配置自动迁移为经典卡片和纯色地块，所有外观偏好即时静默生效。
> GitHub 同步：本轮以最新 `origin/main@4d7c797` 为基础，无改史整合 V5.9 系列管理员增量；推送目标仅为 GitHub `main`，不修改 `origin/admin` 或服务器部署目录。
> 农户灌溉规则：`e8187ed` 已将农户与农场管理员调整为当前操作人确认后直接执行虚拟灌溉；正式虚拟执行收到成功/部分成功 ACK 后写入模拟遥测，失败/超时不改变湿度。
> 管理员“灌溉调度”同时保留 `admin` 分支的固定日配额、余额/预留/实际使用统一事实源、AI 配水、自动灌溉排程、控制器 ACK 结算和 `MANUAL_IRRIGATION` 人工兜底；旧资源接口继续兼容。
> 2026-08-30 地块类型与农务工单影响（本地实现，待用户验收）：地块支持露地（裸地）、大棚、遮阳棚和果园，模拟器/预测按设施的雨水暴露、气候缓冲和光照透过率产生差异；管理员创建的播种、移栽、施肥、植保、除草、整枝、灌溉、采收、巡田和设备检查工单在验收通过后写入对应地块状态、阶段、最近作业和履历，失败/超时只记录未改变，传感器遥测保持原始来源。历史过多时预测曲线默认聚焦最近历史与预测区间，并可用缩放查看完整历史。
> 2026-08-30 管理员分支增量：农场管理员新增独立全宽 AI 助手与账号隔离会话、真实图片浏览器端分析；告警闭环新增农场级学习案例、规则候选、离线验证和人工启用；Crop Pack 同时保留三角色共享全局目录与农场级草稿、校验、启用、归档能力。
> 2026-08-31 系统管理员分支增量：保留独立 `sysadmin.html` 入口和系统管理员经典界面，平台地块态势显示业务名称；新增平台 Agent、账号级历史与删除，决策审计/仿真历史分页，采样间隔和时间流速可直接输入，演示地块场景刷新后保持。
> 2026-08-31 农户分支增量：农户首屏不再被预测、灌溉或助手等次要接口阻塞；风险预测使用 ECharts、指标切换、同快照双轨和实际水量/泵速/水箱约束，轮询时保持当前曲线；灌溉执行不设置时间冷却，低于 10% 的合格土壤遥测可自动发起虚拟浇水，农智助手保留人工确认闭环。模拟遥测统一由 API 进程内 `SimulationEngine` 生成，旧 Python 模拟器目录移除。
> 2026-08-31 农户分支增量：农户首屏不再被预测、灌溉或助手等次要接口阻塞；风险预测使用 ECharts、指标切换、同快照双轨和实际水量/泵速/水箱约束，轮询时保持当前曲线；灌溉不设置时间冷却，农智助手在对话内完成受控确认/取消。模拟遥测统一由 API 进程内 `SimulationEngine` 生成，旧 Python 模拟器目录移除。
> 2026-08-31 低湿自动浇水（T-108，待验收）：灌溉 `cooldownMinutes` 置零并将告警静默期拆为 `alertCooldownMinutes`；新增 `/api/v1/irrigation/auto` 和遥测事件触发逻辑，合格土壤含水量低于 10% 时自动创建带事件幂等键的虚拟灌溉命令，失败/低质量/设备异常/资源不足返回阻断原因，不连接真实水泵。农户灌溉页显示阈值、当前读数和自动触发状态。API Gradle 70/70、Web 定向 18/18、Vite、JS 语法和差异检查已通过；状态仍待用户验收。
> 2026-08-31 三角色 AI 与呈现增量（T-105，待验收）：地块作物图片成为默认关闭的个人显示偏好；农户、农场管理员和系统管理员实际 AI 入口统一为同一聊天外壳，角色画像分别约束快捷问题、回答语气、数据范围和受控操作。系统管理员的快捷回答读取平台服务、跨农场风险、规则与策略状态，农场管理员读取当前农场运营，农户只读取本人地块与任务。规则集继续由启用 Crop Pack 提供确定性阈值；已启用策略候选现在会在农场管理员告警智能处理时显示匹配策略与合格案例数，但仅作为预览，不改变 80% 可信度门槛、安全门和人工确认。发布编译门禁中发现的诊断原因中文映射同名方法已收敛为单一实现；按用户要求未运行测试套件，仅保留编译与轻量静态检查证据。
> 2026-08-31 经典卡片按钮可读性修复（T-106，待验收）：经典材质继续用于普通中性控件，但不再覆盖主操作和选中状态的语义底色；农户工作台的绿色主按钮、可辨认禁用态、消息中心全部/告警/任务/通知选中项及曲线范围选中项均恢复高对比显示。样式入口缓存版本已刷新；按用户要求不运行测试套件，仅保留差异空白和冲突标记静态检查。
> 2026-08-31 农户灌溉补证与自动浇水开关合并（T-111，已完成，本地验收）：正常湿度达到阶段目标且处方水量为零时返回 `NO_ACTION`，农智助手明确无需灌溉和补证；灌溉卡标题收敛为“自动浇水”，新增按地块保存的开启/关闭确认按钮，关闭后自动触发返回 `DISABLED`，开启后恢复低湿度自动触发。异常质量、传感器漂移、设备离线、资源或权限不满足时继续保留确定性硬安全门；演示/正式 API、Web 定向回归、Vite 构建和本地浏览器按钮交互已通过。
> 2026-08-31 V5.8.6 管理员总览 Emoji 作物身份与十一项指标合并（T-113，待用户验收）：在最新 `main` 的 AI 助手、告警治理、Crop Pack、农户自动浇水、系统管理员及工作台偏好基础上，保留浅色 Emoji 作物身份区，并将地块指标固定为土壤湿度、空气温度、空气湿度、光照、二氧化碳、降雨量、酸碱度、水位、速效氮、速效磷和速效钾十一项；正式缺失值仍显示 `—`，接口、权限、详情与质量合同不变。原 `admin` 分支的 `T-089/T-090` 因 `main` 已占用而统一重编号为 `T-113`。合并后 Web Node 94/94、Vite 生产构建通过；浏览器验证 7 张卡共 77 项，桌面 `3-3-2-3`、698px 收起导航后 `2-2-2-2-2-1`，无横向溢出或控制台 warning/error；服务器 `/farm-admin/` 已保留相同界面。
> 2026-09-01 管理员任务、告警联动与工作台偏好收口（T-114～T-116，双服务器已发布）：逾期任务的一键处置和一键重新分配均排除原负责人，只在找到其他具备地块权限的在岗农户后转入进行中；从农场成员进入任务页会携带并保留执行农户与全部状态筛选。告警顶部 AI 智能处理和批量核查必须先选择待审核告警，单条核查保持可用，卡片来源与操作按钮分层排版。工作台删除柔和玻璃和地块图片选项，旧偏好自动归一为纯色，设置和恢复默认均不弹成功提示。合并最新 `origin/main` 后 Web Node `99/99`、Vite 构建和 `git diff --check` 通过；`/farm-admin/`、`/agriloop/` 首页与关键静态模块均为 200，公网健康检查 `UP`。
> 2026-09-01 AI 助手输入框视觉修复（T-117，本地）：移除 textarea 继承的全局焦点阴影、原生边框和内框，只保留外层圆角输入容器的焦点反馈；刷新模块缓存版本。Web Node `99/99`、Vite 构建和 `git diff --check` 通过。
> 2026-09-01 设备卡片与 Codex 风格 AI 助手重构（T-119，本地待验收）：设备台账卡片将运行状态、绑定状态和三点菜单固定为水平分区，保留名称/类型编辑与离线未绑定安全删除门；AI 助手改为保留全局导航的全屏工作区，按授权地块折叠分组会话，新对话仅在侧栏顶部，置顶/归档/删除在悬停或聚焦时显示，已归档入口移入账号菜单。会话支持 `pinned` 字段、地块上下文路由和演示/正式账号隔离；Web Node `101/101`、Vite 构建和 `git diff --check` 通过，Gradle 因本机缺少 JDK 17 编译器待补跑，未部署服务器或推送 GitHub。
> 2026-09-01 农户人工浇灌与无需灌溉界面拆分（T-117，本地验收）：新增 `POST /api/v1/irrigation/manual` 与 `manualFallback` 响应字段；数据冲突、质量、设备或就绪度阻塞时可在农户端打开独立“人工浇灌”弹窗，保留权限、强制人工确认、幂等、单次/每日/资源/时长约束，沿用虚拟命令、ACK、效果评价和模拟遥测回写；成功/部分成功按实际用水量更新土壤湿度，失败/超时不更新，无数值基线标记 `INCONCLUSIVE`。`NO_ACTION` 改为仅展示湿度目标、确定性原因、支持/反对证据和就绪度的只读面板，不进入四步执行流程。后端全类 JUnit 71/71、农户定向 Node 8/8、Vite 构建和 OpenAPI YAML 解析通过；浏览器复核了本地农户主页面与 `NO_ACTION` 只读弹窗，全部动作仍为 `SIMULATED`，不连接真实水泵或阀门。
> 当前周期：15 天软件仿真交付后的持续开发

> 2026-08-26 地块级模拟策略增强（本地+远端已验收）：模拟器按地块热加载独立场景和有界随机波动，支持正常/干旱/暴雨/传感器漂移/设备离线；风险预测与灌溉建议复用策略参数，地块详情提供参数滑块、即时预测预览和历史/预测重置，硬件 `REAL/HARDWARE` 数据按地块优先且重置不删除。风险与农户 SVG 曲线支持鼠标局部数据浮窗。Gradle API、Python simulator、Node 19/19、Vite、stub/svg/real 三种 Web 回归和 OpenAPI YAML 解析通过；远端已按当前发布提交部署，a01 干旱与 a02 暴雨黑盒验证显示温湿度/土壤湿度/降雨量反向变化，硬件离线安全门和缓存版本号也已验证。详见 `docs/simulation-strategies.md`。
> 2026-08-26 告警冷却、在线改密、成员生命周期、巡田照片与高温告警（本地实现，待测试验收）：`evaluateRuleForEvent` 对同地块同规则的 ACTIVE/ACKED/ESCALATED 告警在冷却期内复用并发布 `alert.updated`；关闭后冷却期内不再新建。气温超阈对称落 `HEAT_STRESS_RULE` 告警。新增 `POST /auth/change-password`（校验旧密码、轮换 `credentialVersion`、签发新 JWT）。农场管理员可 `POST /farm-members` 创建农户、`PATCH .../status` 启用/停用。巡田照片以本地附件引用保存，`provenance=USER_PROVIDED`，不是生产级对象存储。该历史条目未覆盖真实执行器；当前 BearPi 遥测接入以本文件顶部口径为准。
> 2026-08-26 农户首页任务优先级与建议安全闭环（历史规则，已由 `T-084` 调整）：首页新增“今天先做什么”三项优先事项，统一进入任务、风险、设备和灌溉建议流程；当时灌溉确认只提交管理员审批，结果表单记录状态、备注和可选实测数据。当前恢复状态仍必须等待最新后端/遥测/效果评价确认。首页地块卡仅保留健康摘要，详细指标与历史曲线仍在地块详情。
> 2026-08-26 系统管理员平台总览空白修复（本地+远端浏览器已验收）：修复 Vue 3 总览地块指标循环的 `v-for`/`v-if` 作用域错误；正式会话恢复四项平台指标、服务健康矩阵、模拟器状态、实时地块卡片和事件摘要，过滤高频遥测并屏蔽 epoch 占位时间，入口资源缓存版本已刷新。详见任务 `T-072`。
> 2026-08-26 地块详情多指标曲线与预测连续性修复（本地+远端验收）：曲线指标扩展为土壤湿度、温湿度、光照、CO₂、pH、水位和降雨量八项，纵轴按单位自适应；预测 0 分钟点与历史末点共享时间和值，重置预测不再因 `Date.now()` 或短窗口斜率出现漂移；演示数据按指标生成有界波动。远端 8 项接口与浏览器切换/重置均通过。详见任务 `T-073`。
> 2026-08-26 三角色实时刷新增强（本地+远端验收）：正式会话增加 SSE 断线指数退避重连、REST 轮询兜底、网络恢复探测和可见性暂停；农场管理员/系统管理员业务数据按域刷新，农户端遥测 5 秒、工作区及风险/资源建议 15 秒兜底，最新遥测直接更新卡片，地块曲线/指标弹窗打开时自动刷新。农户资源建议改为后端只读试算，不创建管理员排程或发布决策事件。远端三角色代表指标在 2.4～4.9 秒内自动变化，曲线连续两个周期取得新数据，页面无 4xx/控制台错误。详见任务 `T-074`。
> 2026-08-27 三角色前端中文化与图标统一（本地验收）：导航、状态、来源、服务、事件、诊断、仿真、账户和农户工作台动态文案统一为简洁中文；后端/演示文本在展示层转换，账号、设备、追踪编号、协议名、模型名和标准计量单位保持技术可追溯性。移除远程 Material Symbols 字体，管理员与农户页面统一使用本地 Phosphor 图标；Node 27/27、Vite、Web stub/svg/real 20/19/20 和 Chromium 三角色页面检查通过。详见任务 `T-075`。
> 2026-08-26 AI 告警分析与智能处理（已合入 main）：农场管理员入口改名为“AI告警分析与智能处理”；保留 main 的自适应概要卡和居中详情弹窗，只保留全选、AI智能处理和一键关闭告警，不再设置重复的直接下发入口。AI 使用诊断置信度、缺失证据和规则状态判定告警可信度，只把高可信告警按地块权限、当前任务量和过往地块经验直接下发给合适农户，证据不确定或无合格农户时保留人工审核。单条/批量关闭采用乐观状态：点击瞬间从待审核、已下发和全部未关闭列表移除，失败才恢复；刷新回来的旧状态也会与关闭结果合并，本地演示告警状态持久写回，并兼容 `id`/`alertId` 两种主键。告警与对话模块的重复大标题已移除；对话页改为经典单列会话布局，新对话时在中央显示四个快捷问题，输入框固定在底部。详见任务 `T-076`。
> 2026-08-27 农户端 P0 创新缺口闭环（farmer-ui 分支，本地验收完成；历史审批规则，直接执行由 `T-084` 覆盖）：补齐阶段目标带与只读阶段预览、指标质量三维字段、诊断支持/反对/缺失证据、就绪度补证、迟滞/冷却保护、冻结快照双轨、审批工单到 ACK/效果只读链路以及 RAG/工具审计；管理员从审批工单加载原处方，现行农户直接执行规则另行记录。Gradle 50/50、Node 40/40、Vite、OpenAPI 和桌面/390px 浏览器集中验收通过，控制台无 warning/error。详见任务 `T-081` 与 `docs/acceptance/FARMER_P0_ACCEPTANCE.md`。
> 2026-08-27 农户地块展示收口（代码与自动化验收）：趋势图移除土壤电导率，批次阶段标题隐藏 `I-19` 编号，地块概览、任务详情和作物包指标统一将指标编码映射为中文；Node 51/51、Vite 构建通过，浏览器页面待在 HTTP 本地地址复核。详见任务 `T-082`。
> 2026-08-28 农户灌溉自主执行规则调整（本地+服务器验收）：农户与农场管理员共享 `irrigation:execute` 能力，灌溉建议按钮改为“查看建议并执行”；当前操作人确认后直接创建虚拟命令，不再创建管理员审批工单。安全门、地块鉴权、用水上限、冷却窗口、ACK、效果评价和幂等键保持不变；旧 `REQUEST_APPROVAL` 接口和已有审批记录继续兼容。`e8187ed` 已部署至 `/srv/agriloop/app`、`/srv/farmer/app`、`/srv/farm-admin/app` 和 `/srv/system-admin/app`，远端健康检查、静态入口和三角色权限复核通过。
> 2026-08-26 AI 告警分析与智能处理（已合入 main）：农场管理员入口改名为“AI告警分析与智能处理”；保留 main 的自适应概要卡和居中详情弹窗，只保留全选、AI智能处理和一键关闭告警，不再设置重复的直接下发入口。AI 使用诊断置信度、缺失证据和规则状态判定告警可信度，只把高可信告警按地块权限、当前任务量和过往地块经验直接下发给合适农户；当时的不确定项审核方案已由 2026-08-27 现场核查闭环替代。单条/批量关闭采用乐观状态：点击瞬间从待审核、已下发和全部未关闭列表移除，失败才恢复；刷新回来的旧状态也会与关闭结果合并，本地演示告警状态持久写回，并兼容 `id`/`alertId` 两种主键。告警与对话模块的重复大标题已移除；对话页改为经典单列会话布局，新对话时在中央显示四个快捷问题，输入框固定在底部。详见任务 `T-076`。
> 2026-08-27 农务任务与告警核查闭环（本地+服务器静态资源/API 验收，待用户正式页面验收）：管理员任务导航统一为“进行中、待分配、待验收、已逾期、已完成”，返工只作为进行中卡片标记；逾期页同时提供一键重新分配和一键处置，两者写入新的未来时限后立即退出逾期页。告警页增加一键发布核查任务，AI 不确定项不再停留在人工审核；核查任务验收时以“确认异常/现场正常”为唯一结论，前者自动创建并分配处置任务，后者自动关闭原告警。AI 处理期间详情弹窗仍可随时退出。演示数据覆盖 7 条告警和两条逾期任务。Node 58/58、Gradle、Vite、Web real/stub/svg 38/38/37 通过；公网三项版本资源均返回 200，正式管理员登录和任务/告警只读接口通过。
> 2026-08-27 农户端 P0 创新缺口闭环（farmer-ui 分支，本地验收完成）：补齐阶段目标带与只读阶段预览、指标质量三维字段、诊断支持/反对/缺失证据、就绪度补证、迟滞/冷却保护、冻结快照双轨、审批工单到 ACK/效果只读链路以及 RAG/工具审计；管理员从审批工单加载原处方，农户保持无命令权限。Gradle 50/50、Node 40/40、Vite、OpenAPI 和桌面/390px 浏览器集中验收通过，控制台无 warning/error。详见任务 `T-081` 与 `docs/acceptance/FARMER_P0_ACCEPTANCE.md`。
> 2026-08-27 管理员界面分支增量已组合：生产计划与 Crop Pack 使用概要卡和居中详情弹窗；农务任务按生命周期、类型和重点关注分类；设备与设施页增加概况、组合筛选、设备详情及关联告警/设备检查任务；用水试算与价值对账集中到农务任务的“资源安排”页签；旧地址继续兼容回落。上述增量与主分支实时刷新、AI、中文化和模拟器入口共同保留。
> 2026-08-27 设备与设施白屏及开关控制（本地+服务器已部署，待用户验收）：修复真实设备数据到达后 `sourceLabel` 未暴露导致的 Vue 渲染白屏；设备卡片与详情增加安全开关，模拟设备切换后立即更新并由模拟器停止/恢复遥测，REAL/HARDWARE 设备通过 MQTT 控制指令和 ACK 回执更新实际状态，失败/超时保留原状态并记录原因。新增 `POST /api/v1/devices/{deviceId}/control`、幂等命令、控制超时、遥测/心跳状态保护及 BearPi/模拟器控制订阅；Node、Gradle、Python、Vite 和 Web real/stub/svg 回归通过，服务器模拟设备已完成“离线→在线”黑盒并恢复原状态，未操作真实 BearPi。
> 2026-08-28 现有服务器原地升级与完整验收：先备份 `/srv/agriloop`、`/srv/farm-admin` 至 `/srv/backups/agriloop-20260828-110354`，同步 `main@601fe08` 并重启 API、模拟器和 Nginx。服务器 `:api-service:test` 52/52、bootJar、API `UP`；公网管理员登录、模拟设备绑定后离线/在线控制、跨地块转移、单条核查幂等、Agent 预览/确认/取消/幂等/越权、核查正常自动关闭告警均通过。新增修复：真实设备首条遥测不再被初始 OFFLINE 控制状态误抑制，只有收到实际控制命令后才按确认离线保护。未操作真实 BearPi。
> 2026-08-28 GitHub 主分支同步：本地 `main` 合并 `origin/main@34a6a11` 生成 `4e9326a`，已快进推送至 `https://github.com/FlowerNeverFade/smart-agriculture.git` 的 `main`；仅整合远端提交与交接记录，不改变已验收功能。
> 验收数据留存：Agent 验收地块因模拟器已写入遥测历史而无法物理删除，已停用并保留“名称 Agent验收临时地块”标识；验收模拟设备均已解绑，未影响生产硬件。
> 2026-08-28 Agent 确认卡修复：运行时编译组件不再把 `ref` 对象直接绑定到按钮 `disabled`，确认执行/取消均可点击；前端增加缓存版本号。后端允许历史缺少 Crop Pack 元数据的地块进行字段级局部修改，避免 Agent 确认更新地块时被无关字段校验拦截。公网实时会话已验证取消和确认均成功，确认后刷新相关数据域。
> 2026-08-29 资源分配与风险推演重构（本地 `V5.8`，待用户验收）：新增 `POST /api/v1/forecasts/evaluate` 只读确定性试算合同，显式要求地块上下文、回显请求版本、限制参数范围且不写入预测或策略；地块详情以 300ms 防抖和请求序号丢弃过期结果，未保存参数只刷新后端试算曲线。管理员“资源安排”整合真实用水事实、当前/未来配额、风险需求、设备与时间窗、容量试算、缺口、逐行核对、原因必填的人工调整及用水账本。Node 68/68、Vite、OpenAPI YAML 与浏览器主要闭环通过；本机 Gradle 仍因 Java Runtime 不可用未进入断言。
> 2026-08-30 农户“更多工具”增强（待用户验收）：农户端将作物培养手册归入“更多工具”，风险预测页签改为仿照农场管理员地块详情的未来预测视图；仅选择负责地块，复用同一套历史遥测、管理员策略、指标切换和 `getRiskForecast` 预测曲线，并支持参数调整后的本地只读试算，不写回管理员策略；样本不足时明确显示不可用。修复图表切换页面后的空白问题，以及加载/实时刷新时用 `v-if` 拆掉画布导致的曲线闪烁：改为常驻 ECharts 容器、静默刷新和轻量遮罩。另为正式 API 请求增加 5 秒超时，后端未启动或连接悬挂时进入现有降级路径，不再卡在 88% 加载阶段。培养手册目录继续从 `MOCK_DATA.cropPackDetails` 展示全部作物与阶段资料。前端 Node 74/74、Vite、语法和静态检查通过。
> 2026-08-30 模拟时间与灌溉窗口修正（待用户验收）：默认时间倍率改为 `144x`（墙上时钟 10 分钟 ≈ 1 个模拟日），上限 `288x`；土壤、温湿和降雨按每模拟小时的自然日变化率演化，干旱约失水 `11%/模拟日`，正常约 `3%/模拟日`。旧未显式保存的 `1x` 配置升级为 `144x`。灌溉建议窗口仍为生成后 `2`～`12` 分钟。
> 2026-08-30 虚拟灌溉按处方水量回写湿度（待用户验收）：成功/部分成功后土壤湿度增量改为 `waterLitre / (areaM2 × 0.08)`，不再使用固定 +10%；水位按 900L 水箱容量下降。农户曲线默认窗口改为 1 个模拟日，横轴按时间倍率标注模拟时刻。
> 2026-08-30 新增生菜与茄子培养指导（待用户验收）：依据农业农村部 2025 年夏季蔬菜指导意见、2026 年生菜设施栽培要领和北京市大棚茄子技术要点，新增两套四阶段演示/正式 Crop Pack，包含阶段目标、指标、缺水/高温/低温规则、知识来源和数据质量降级说明；详见 `docs/crop-guidance-sources.md` 与任务 `T-090`。
> 2026-08-30 后端内置模拟器与管理员流速控制（待用户验收）：模拟遥测由 API 进程内 `SimulationEngine` 直接 `ingest`（`sourceMode=SIMULATION`），原 Python `simulator/` 目录已移除。系统管理员「仿真模拟」页可调采样间隔（5–60 秒，默认 20）和时间流速（1–288x，默认 144），启停改为内存引擎。MQTT 仍保留给真实硬件与命令桥。详见任务 `T-093`。
> 2026-08-30 农户手册与后端目录同步（待用户验收）：正式 API 从 4 个扩展为 9 个 Crop Pack，补齐草莓、辣椒、玉米、水稻和向日葵；农户正式会话的手册目录与 `mock-data` 保持一致，每个作物均提供四阶段目标、指标、规则、场景和知识文档。详见任务 `T-092`。
> 2026-08-30 虚拟灌溉效果回写修正（待用户验收）：演示与正式虚拟执行的成功/部分成功都会按处方水量与地块面积写入土壤湿度（`Δ湿度 = waterLitre / (areaM2 × 0.08)`）和水位模拟遥测；正式农户端等待 ACK/效果评价后刷新工作台与遥测窗口，失败/超时不改变湿度。
> 2026-08-30 合入远端 `farmer-ui` 农户主面板信息层级（本地验收，任务编号 T-094）：首页首层改为“今天先做什么 + 当前风险”，统计卡压缩为状态条，地块概览提前到辅助信息之前，任务与普通通知合并为“最近动态”，告警集中到优先行动和风险摘要；窄屏默认收起侧栏并保持单列。详见 `docs/acceptance/FARMER_DASHBOARD_ACCEPTANCE.md`。
> 2026-08-30 合入远端 `farmer-ui` 农智助手（本地验收，任务编号 T-095）：移除左下角咨询弹窗，新增 `#assistant` 单列连续对话、四个快捷问题、历史会话抽屉、按需依据/执行记录和 390px 响应式布局；写操作先生成 10 分钟预览，灌溉仅接受 `READY` 处方。详见 `docs/acceptance/FARMER_ASSISTANT_ACCEPTANCE.md`。
> 2026-08-30 农户反馈修复（T-096，历史规则，已由 T-108 取代）：曾为普通冷却与严重干旱应急分流；当前灌溉不设置时间冷却，自动浇水阈值与事件触发由 T-108 统一收口。聊天内仍保留人工灌溉预览、确认和取消，并显示具体阻断原因。

## 1. 当前结论

AgriLoop 的数据、智能体和可视化三条软件主线已形成可运行闭环。后端、数据库迁移、Crop Pack、模拟器、三角色权限、管理员管理能力及指定前端切片均已进入 GitHub `main`。当前基线同时接收进程内模拟遥测和 BearPi E53_IA1 的 `REAL/HARDWARE` 遥测，按地块和指标做来源仲裁；灌溉命令仍走虚拟执行器。BearPi 链路已纳入当前联调/验收，其他现场传感器、网关、真实执行器和鸿蒙端不因该适配而自动视为完成。

| 范围 | 当前状态 | 说明 |
| --- | --- | --- |
| Git 功能基线 | 本地 `main` | 已包含最新 `origin/main` 和本轮任务/告警/设置及农户地块排序增量；本轮只创建本地提交，不推送 GitHub 或部署线上，保留原有 `.gitignore` 用户修改 |
| 角色 | 已完成 | `FARM_ADMIN`、`FARMER`、`SYSTEM_ADMIN` 三角色；旧角色只保留兼容迁移，不作为第四种界面身份 |
| 数据主线 | 已完成 | 模拟遥测由 API 进程内引擎生成并 ingest；BearPi 真实遥测走 MQTT -> Redis Streams -> PostgreSQL -> SSE，含校验、去重、质量、新鲜度、来源仲裁和恢复 |
| 智能体主线 | 已完成 | 确定性诊断/处方/安全门为事实边界，Qwen 只负责受控解释；不可用时明确降级 |
| 管理员界面 | 已完成当前实现 | 二维指标总览、任务/告警/设备概要卡、任务五段状态、逾期双动作、AI 高可信派单、现场核查闭环和完整 AI 对话页均已落盘并在公网静态资源验收 |
| 农户界面 | P0 创新闭环与 T-117 人工浇灌/无需灌溉分流已完成（本地验收） | 授权地块、阶段目标带、指标质量、诊断证据、就绪度补证、迟滞、无时间冷却、低于 10% 自动浇水（含按地块开关）、直接执行与 ACK/效果只读链、人工兜底与土壤湿度效果回写、Agent 审计和今日农务均已验收；`NO_ACTION` 使用独立只读原因面板；“更多工具”包含管理员地块详情同款的负责地块未来预测和完整作物培养手册；首页已按行动→风险→地块→辅助信息→动态重排；`#assistant` 连续对话、会话抽屉、四项农户白名单工具和来源标识已在桌面与 390px 窄屏复核；地块遥测可能来自 BearPi `REAL/HARDWARE` 或 `SIMULATION`，灌溉结果仍为虚拟执行，不代表真实水泵/阀门动作 |
| 系统管理员界面 | 已完成当前实现 | 平台总览、审计、规则、策略、模拟器和账号范围保持远端合并结果；总览渲染错误已修复并通过正式会话浏览器验收 |
| Crop Pack | 已实现，页面待验收 | 番茄、黄瓜阶段解析、模板、知识、规则和综合健康分已接入 |
| 公网部署 | 已完成（双服务器静态验收） | `/srv/farm-admin` 与 `/srv/agriloop` 均记录 `a794e9e7400ed86b248cbdc639c09a9346213a31`；Nginx 配置通过，两个公网入口和关键模块 200，API `UP`；未重启后端。 |
| 答辩物料 | 未开始 | PPT、录屏和最终归档不在当前代码开发范围 |

## 2. 当前管理员产品状态

- 左上角只保留菜单按钮和 AgriLoop Logo，不显示 `AgriLoop Console`。
- 顶栏使用一个全局农场选择器；切换后统一刷新地块、任务、告警、设备、成员、批次和账本，并阻止旧请求覆盖新农场状态。
- 管理员告警导航名称为“告警智能处理”，并新增仅 FARM_ADMIN 可见的“AI助手”；告警处理与对话入口分离，旧聊天 Hash 地址自动兼容跳转。
- 农场总览与地块管理已合并；今日概况、地块状态、编辑入口和添加地块卡片同屏展示，不再显示重复二级页签。
- 地块使用二维六指标卡片与二维详情，不恢复旧 3D FarmMonitor。
- 五项统计是可操作按钮，并保留当前 `farmId`：
  - 今日任务 -> 今日农务；
  - 已逾期 -> 逾期任务；
  - 异常地块 -> 告警处置；
  - 待分配 -> 待分配任务；
  - 待审批 -> 待审批任务。
- 已知环境指标在管理员总览、地块详情、诊断、告警来源和 Crop Pack 阈值表中优先显示简洁中文，接口编码保持不变。
- 农务任务导航和状态下拉统一为“进行中、待分配、待验收、已逾期、已完成”；返工任务回到进行中并保留返工标记，不单设导航。
- 已逾期页提供全选、一键重新分配和一键处置；两种批量动作都必须转交给其他具备地块权限的在岗农户，找不到替代人员时保留原任务并说明原因，成功后更新处理时限并转入进行中。
- 农场管理员的任务列表使用自适应概要卡网格；页面只保留外层“农务任务”主标题，任务操作区、概况和筛选器与页签统一左边缘；点击卡片或使用键盘可打开居中详情弹窗，完整结果、操作记录和分配/验收/取消入口保留在详情中。农户和系统管理员仍使用原有完整任务卡片及自身标题。
- AI 告警页保持 `auto-fill/minmax(280px, 1fr)` 概要卡网格和居中详情弹窗；顶部 AI 智能处理与批量发布核查任务只有选中至少一条有效待审核告警后才启用，卡片/详情中的单条核查保持可用；来源和操作按钮分层展示，AI 运行期间详情仍可退出。
- AI 智能处理只自动下发可信度达到 80%、证据完整且诊断可用的告警；其余告警进入现场核查任务，不再以人工审核作为终点。
- 核查任务由农户提交结果与巡田证据；管理员确认“异常”后系统自动生成并分配后续处置任务，确认“正常”则自动关闭原告警，核查结论是唯一后续依据。
- 直接下发与现场核查都复用现有工单创建与分配合同，只选择在岗且有地块权限的农户，并按当前进行中任务量、过往地块处理经验排序；暂时无合格农户时保留为待核查状态，待人员条件满足后再发布核查任务。
- 农场管理员原诊断/灌溉二级页替换为经典单列 AI 对话页；移除重复大标题，保留地块上下文、历史对话、新对话和降级提示，新对话中央直接显示快捷问题，底部使用圆角悬浮输入框，不自动执行设备操作。
- 管理员界面采用克制的 macOS 风格层级：顶栏和侧栏保留轻材质，内容卡片使用清晰白色表面；任务、逾期、异常、设备和成员使用可辨识的语义色，但仍保留图标与文字，不增加装饰性背景图。
- 第 2、3 轮能力已接入：设备注册/绑定/解绑、地块生命周期、农户成员与地块范围、种植批次与生产计划、Crop Pack、价值对账、模拟器状态、巡田证据和跨模块闭环。
- “灌溉调度”已把配额事实、风险/需求队列、只读预测曲线、多地块申请与分配、容量缺口、设备与执行窗口、人工调整原因和用水事实账本放入同一核对面；所有试算均明确不扣减真实库存，灌溉执行仍进入农务任务闭环。
- 风险推演不再是独立导航：管理员在地块详情调整场景或参数时调用后端只读试算，只有“保存到此地块”才更新策略；农户旧 `tools/risk` 地址规范化回主面板，管理员旧 `risk-forecast` 地址回到农场总览。
- 远端新增的农户创建/启停、告警冷却与高温告警、在线改密及最多 6 张巡田照片已保留；巡田照片标记为 `USER_PROVIDED`，当前仅是本地附件引用，不表述为生产级对象存储。

## 3. 核心闭环与边界

当前业务闭环为：

```text
生产计划
  -> 今日农务
  -> 遥测/田间核验
  -> 根因诊断
  -> 决策就绪度
  -> 结构化灌溉处方
  -> 人工确认
  -> 虚拟命令与 ACK
  -> 效果评价
  -> 计划实绩与回放
```

- `SENSOR_DRIFT`、设备离线、坏/过期/低质量数据不会生成可执行灌溉结论；正常低风险人工确认场景中的证据不足或人工冲突可以只作为 `advisoryEvidence`，仍需操作人确认且不代表证据消失。
- `FAILED`、`PARTIAL`、`TIMEOUT` 与 `SUCCEEDED` 分开保存和展示。
- 工单主链为 `OPEN -> ASSIGNED -> IN_PROGRESS -> SUBMITTED -> DONE`，退回链为 `SUBMITTED -> REJECTED -> IN_PROGRESS`。
- 巡田记录保留人员、时间、来源、质量和任务引用，不覆盖遥测事实。
- 资源试算不超过容量，不回写真实剩余水量；价值账本不在缺少证据时推导真实利润。
- 模拟设备、虚拟执行和演示数据必须继续显示 `SIMULATED`、`ESTIMATED`、`DERIVED` 或其他明确来源。

## 4. 当前待验收任务

| 任务 | 状态 | 当前实现与剩余验收 |
| --- | --- | --- |
| T-061 | 待验收 | Crop Pack 阶段解析、综合健康分和培养手册接口已实现；待用户检查手册作物/阶段切换及农户健康分展示 |
| T-062 | 待验收 | 管理员总览与地块管理已合并；待用户检查页面布局和交互 |
| T-063 | 待验收 | 设备绑定即时显示、首次心跳提示及种植农户增删改已实现；待用户检查正式页面 |
| T-064 | 已完成 | 五项总览统计已接入真实 Hash 路由、农场上下文和任务范围筛选 |
| T-065 | 待验收 | 农户端天气风险、设备核验、批次阶段、水量分配、降级、报告和案例确认呈现已由远端合入，待用户检查 |
| T-066 | 待验收 | 告警冷却、高温告警、在线改密、成员创建/启停与巡田照片附件已由远端合入，待本地回归与用户检查 |
| T-067 | 已完成 | 地块独立模拟策略、随机波动、曲线重置与局部浮窗已通过本地及远端验收 |
| T-068 | 待验收 | 管理员指标中文化、农务任务逾期页签和告警降级处理已修复；自动化测试与构建通过，待用户在本地页面确认 |
| T-069 | 待验收 | 管理员农务任务已改为自适应概要卡与居中详情弹窗；农户和系统管理员布局保持不变，待用户本地检查卡片密度与详情交互 |
| T-070 | 待验收 | 管理员告警与设备列表已改为概要卡网格和详情弹窗，设备注册使用添加卡片；待用户本地检查密度、详情和业务操作入口 |
| T-071 | 待验收 | 仅 `FARM_ADMIN` 增加 macOS 层级化语义色视觉，并收口任务页重复标题与独立居中造成的错位；自动化及浏览器功能/视觉检查通过，业务结构、三角色合同和其他角色界面不变，待用户确认整体观感 |
| T-072 | 已完成 | 系统管理员平台总览修复：改正指标循环作用域、补充正式会话安全降级和实时事件摘要，入口缓存版本已刷新。Node 24/24、Vite、Web stub/svg/real 16/15/16 通过；经 SSH 隧道连接远端正式会话，四项指标、6 个服务卡、地块卡片和事件列表可见且控制台无页面错误。 |
| T-073 | 已完成（本地+远端验收） | 地块详情八项曲线指标、单位自适应纵轴、历史/预测时间和值锚定、正常场景趋势抑制和演示多指标数据已实现；后端连续性回归、Node 24/24、Vite、Web stub/svg/real 19/18/19 与 Python simulator 通过；远端 8 项遥测/预测接口均返回，真实浏览器切换指标及重置预测首点连续。 |
| T-074 | 已完成（本地+远端验收） | 三角色正式页面实时刷新：SSE 断线指数退避重连、可见页面轮询兜底、网络恢复探测、页面隐藏暂停与恢复补刷；农户最新遥测直接更新卡片，风险/资源建议定期刷新且资源试算保持只读；地块曲线和管理员指标弹窗自动更新。Gradle、Node 26/26、Vite、Web stub/svg/real 20/19/20 通过；远端代表指标在 2.4～4.9 秒内变化，农户试算 200，页面无 4xx/控制台错误。 |
| T-075 | 已完成（本地验收） | 三角色前端界面中文化与本地图标统一；导航、状态、来源、服务、事件、诊断、仿真、账户和农户工作台动态文案统一为简洁中文，动态展示层保留技术标识；本地图标已替代远程 Material Symbols。 |
| T-076 | 待验收 | AI 告警页保留高可信智能派单，并增加不确定项一键现场核查；核查结果确认异常后自动下发处置任务、确认正常后自动关闭告警。详情在 AI 处理中可退出；关闭乐观更新与经典 AI 对话布局继续保留。 |
| T-077 | 待验收 | 管理员生产计划与 Crop Pack 概要卡网格及详情弹窗；新建计划、审批、Pack 阶段与指标事实保留原合同和中文指标，待用户确认页面观感 |
| T-078 | 待验收 | 管理员农务任务生命周期、类型与重点关注分类重构；顶层任务概况按六个互斥阶段呈现，任务类型与重点关注独立筛选，来源保留底部溯源，待用户确认分类与顶部布局 |
| T-079 | 待验收 | 管理员总览综合健康色阶与卡片状态解耦；综合健康数字只按分数映射色阶，设备待数据等问题由卡片顶边独立提示，待用户确认色彩语义 |
| T-080 | 待验收 | 管理员设备资产与资源安排职责重构；设备与设施增加概况、组合筛选、详情及关联告警/设备检查任务，用水试算和价值对账迁入资源安排，旧地址兼容回落，待用户确认职责划分与页面效果 |
| T-081 | 已完成（本地验收） | 农户端 P0 创新缺口完整闭环：阶段目标带与质量三维、三类诊断证据、缺失证据补证、迟滞/冷却、冻结快照双轨、旧 `REQUEST_APPROVAL` 合同兼容、命令 ACK/效果只读护照及 RAG/工具审计；直接执行规则由 `T-084` 覆盖。Gradle 50/50、Node 40/40、Vite、OpenAPI、桌面/390px 浏览器验收通过，控制台无 warning/error；详见 `docs/acceptance/FARMER_P0_ACCEPTANCE.md`。 |
| T-082 | 已完成（远端验收） | 设备白屏、模拟/真实安全控制、MQTT ACK、幂等与超时、遥测/心跳保护、卡片/详情开关和来源标签已部署；服务器 Gradle 53/53、bootJar、模拟设备离线→在线及服务重启通过，真实设备首条遥测保护补丁为 `601fe08`。 |
| T-083 | 已完成（远端验收） | 设备来源与双向绑定/跨地块转移、单条告警核查幂等、正常自动关告警、受控 Agent 写操作预览/确认/取消/幂等/越权均通过公网黑盒；确认卡实时点击回归通过，历史地块局部修改校验已补齐；Web Node 61/61、Vite、服务器 Gradle 53/53 通过。 |
| T-084 | 已完成（本地+远端验收） | 农户与农场管理员直接执行虚拟灌溉：共享 `irrigation:execute` 能力，当前操作人确认后直接创建虚拟命令，不再创建管理员审批工单；安全门、地块鉴权、用水上限、无时间冷却、ACK、效果评价和幂等键保持，旧审批接口兼容。Node 51/51、Vite、Web stub/real 31/31、SVG 30/30、Java Gradle 50/50、OpenAPI 解析和本地浏览器闭环通过；`e8187ed` 已发布四个服务器应用目录，公网静态脚本、健康检查、农户/管理员权限复核通过；未触发在线灌溉命令。 |
| T-085 | 已完成（本地验收） | 农户地块展示收口：移除土壤电导率趋势图和批次阶段 `I-19` 编号；概览卡、任务详情及作物包指标统一通过指标编码中文映射显示，未知编码保留原始值以保证可追溯。Node 51/51、Vite、静态检查与 HTTP 页面复核通过。 |
| T-086 | 待验收 | 资源分配与风险推演归并：管理员资源页同屏展示配额事实、需求风险、预测置信区间、设备/窗口、容量缺口、核对/确认/取消、原因必填人工调整和用水账本；地块详情改用不落库的后端确定性试算并处理防抖与过期响应，独立风险页面退出且旧地址兼容回落。Node 68/68、Vite 构建、OpenAPI YAML 和桌面双主题浏览器主要闭环通过；本机 Gradle 因 loopback 启动故障未进入断言。 |
| T-087 | 待验收 | 农户地块详情只读展示管理员维护的模拟策略、当前场景、模拟器状态、参数摘要与策略预测曲线；不开放策略修改，待合并后自动化与浏览器复核。 |
| T-088 | 待验收 | 农户更多工具：仿照管理员地块详情的负责地块未来预测与完整作物培养手册；复用历史遥测、管理员策略、指标切换和 `getRiskForecast` 曲线，支持参数调整后的本地只读试算，不写回策略。 |
| T-089 | 待验收 | 模拟时间流速与灌溉窗口校准。 |
| T-090 | 待验收 | 生菜与茄子阶段化培养手册。 |
| T-091 | 待验收 | 虚拟灌溉后土壤湿度状态回写。 |
| T-092 | 待验收 | 农户手册与后端 Crop Pack 目录同步。 |
| T-093 | 待验收 | 模拟遥测改由 API 进程内 `SimulationEngine` 直接 ingest；系统管理员仿真页可调全局采样间隔与时间流速；原 Python `simulator/` 目录已移除。 |
| T-094 | 已完成（本地验收） | 合入远端 `farmer-ui` 农户主面板任务优先信息层级：首层行动/风险双栏、统计状态条、地块提前、辅助信息后置、任务与普通通知统一动态、告警去重；窄屏默认收起侧栏。详见 `docs/acceptance/FARMER_DASHBOARD_ACCEPTANCE.md`。原分支任务号 T-086 已改编号，避免覆盖本地资源安排任务。 |
| T-095 | 已完成（本地验收） | 合入远端 `farmer-ui` 农智助手：`#assistant` 连续对话、会话抽屉、四项农户白名单工具、10 分钟预览和虚拟灌溉确认闭环。详见 `docs/acceptance/FARMER_ASSISTANT_ACCEPTANCE.md`。原分支任务号 T-087 已改编号，避免覆盖本地地块模拟策略展示任务。 |
| T-096 | 待验收 | 普通冷却与严重干旱分流；应急补水保留数据、设备、资源和权限硬门，仅受控绕过重复冷却；建议确认卡展示具体阻断原因；农智助手灌溉操作在聊天内生成确认/取消卡并显示执行模式。API 59/59、Web 68/68、Vite、语法检查和本地 3001 桌面/390px 浏览器证据已完成，待用户确认。详见 `docs/acceptance/FARMER_ASSISTANT_ACCEPTANCE.md`。 |
| T-108 | 待验收 | 农户今日农务刷新时合并 `/work-orders` 与 `/work-items/today` 的真实工单记录并按 `workOrderId` 去重，确保已完成任务保留在完成列；告警/诊断聚合项不进入普通任务卡。`live-data` 11/11、Vite 构建和静态检查通过，待浏览器刷新闭环验收。 |
| T-111 | 已完成（本地验收） | 合入 `farmer-ui` 的农户灌溉补证简化与自动浇水开关：正常湿度达到阶段目标且处方水量为零时返回 `NO_ACTION`；灌溉卡标题收敛为“自动浇水”，增加按地块保存的开启/关闭确认按钮，关闭后自动触发返回 `DISABLED`，开启后恢复低湿度自动触发。演示/正式 API、Web 20/20、Vite 构建、后端定向测试与本地浏览器按钮交互验收通过。 |
| T-112 | 已完成（GitHub main） | 将服务器 `/srv/farm-admin` 发布标记 `5559a71` 与 `origin/main@c8b7675` 非强制合并；农场总览保留左上角作物身份样式，管理员独立 AI 助手、规则与策略、Crop Pack 治理和账号级外观偏好保留，同时接入主分支原生多模态、农户自动浇水、系统管理员及内置模拟器更新。Web Node 92/92、API Gradle 71/71、Vite 生产构建和差异检查通过。 |
| T-113 | 待验收 | 在最新 `main` 上合入 V5.8.6 管理员总览：保留浅色 Emoji 作物身份区，将地块卡固定为十一项环境与肥力指标；演示 7 个地块均补齐数据，正式缺失值显示 `—`，接口、权限、详情与指标质量合同不变。原分支 `T-089/T-090` 因主分支编号冲突合并重编号。Web Node 94/94、Vite 构建、桌面/698px 浏览器和控制台检查通过；服务器 `/farm-admin/` 已保留相同界面。 |
| T-114 | 已完成（双服务器验收） | 逾期任务处置强制转交其他负责人。 |
| T-115 | 已完成（双服务器验收） | 告警批量选择约束与成员任务跨页筛选。 |
| T-116 | 已完成（双服务器验收） | 工作台仅保留经典卡片/液态玻璃及纯色地块，旧偏好自动迁移。 |
| T-117 | 已完成（本地验收） | 农户端人工浇灌与无需灌溉界面拆分，保持虚拟执行安全边界。 |
| T-120 | 待验收 | 农场管理员总览保持现有卡片结构和十一项指标布局，只强化地块、作物、事实及指标网格边界。 |
| T-121 | 待验收 | 农场管理员总览三项事实与十一项指标统一为同一浅色数据表面。 |
| T-122 | 待验收 | 农场管理员总览十一项指标按卡片宽度自适应为紧凑布局。 |
| T-123 | 本地真实协同验收通过，待用户验收 | V5.9 三账号真实资源同步，包含标签页级会话、文件型 H2、角色范围过滤、数据库拒写降级及 REST/SSE 同步。 |
| T-124 | 待用户验收 | V5.9.1 将农场管理员水资源页面的可见名称统一为“灌溉调度”，路由、接口、权限及同步合同不变。 |
| T-125 | 待用户验收 | V5.9.2 Crop Pack 卡片优先读取 `icon` 并使用统一 Emoji 兜底，九种内置作物与详情交互已核对。 |
| T-132 | 待验收 | 农户灌溉证据门分级：`blockingEvidence` 决定阻断，`advisoryEvidence` 只提示，`executionAllowed` 统一前后端执行判定；正常低风险人工灌溉中的 `MORE_DIAGNOSIS_EVIDENCE`/`HUMAN_EVIDENCE_REVIEW` 可保持 `READY`，坏/过期/漂移/离线/暴雨/资源权限时长失败及自动/应急完整安全门不足仍阻断；证据、冲突和审计不删除。正式 API、演示 API、农户确认弹窗同步，线上未部署。 |
| T-133 | 待用户验收 | 农户任务状态引用刷新与具体问题上报：执行状态同步、关联 `FARMER_REPORT` 工单、管理员总览/任务/动态/SSE 接收；Web 定向 26/26（完整前端 109/109）、Java Gradle 全量、Vite、差异检查和本地浏览器关键路径验收通过；已随 `main@165aefd9` 发布，待线上操作验收。 |
| T-134 | 已完成（本地验收） | 农户端地块稳定排序、账号级跨设备偏好、版本冲突保护、长按拖拽回滚、统一入口顺序和十一项固定指标槽位已实现；详见 `docs/acceptance/FARMER_PLOT_ORDER_ACCEPTANCE.md`，已随 `main@165aefd9` 发布，待线上操作验收。 |
| T-135 | 待验收 | 巡田/复测记录和补证申请保留既有角色与农场隔离、持久化和管理员展示；补证按地块+证据类型+未完成工单去重，重复申请复用；普通巡田无便携仪值只补充现场观察，`RETEST` 必须有便携仪值，匹配复测标记旧冲突 `RESOLVED`，新冲突标记旧冲突 `SUPERSEDED` 并保留历史；保存后前端只走一次处方→readiness→诊断刷新。线上未部署，详见 `docs/acceptance/FARMER_P0_ACCEPTANCE.md`。 |
| T-126 | 待用户验收 | V5.9.3 农场管理员日度市场行情：授权农场范围、官方日价、持久化快照、真实缺失断线、九种 Crop Pack 映射、市场比较与非自动交易销售观察。 |
| T-127 | 已完成（历史交付，国内展示由 T-130 收口） | V5.9.4 的英国 DEFRA 原始数据、单位、来源和缺口合同继续供全球批发伦敦节点使用；国内行情中的参考曲线入口已退出。 |
| T-130 | 已完成（历史交付，报价覆盖由 T-131 扩展） | V5.9.7 完成国内/国际展示隔离；V5.9.8 继续保持国内无国际参考，并按用户要求增加全国官方后备和独立模拟趋势。 |
| T-131 | 待用户验收 | V5.9.8 国内行情使用重庆官方报价优先、全国官方报价后备；真实历史不足时显示可关闭的独立模拟趋势，模拟值不落库、不参与任何真实统计或销售观察。 |
| T-136 | 待用户验收 | V5.9.9 开放三角色注册并以服务端授权码受控创建 SYSTEM_ADMIN；SYSTEM_ADMIN 使用真实全局账号接口管理非系统账号，系统账号永久保护，农场成员接口保持 Farmer-only。V5.9.9 的自注册 FARM_ADMIN 示范农场范围已由 T-137 替代。 |
| T-137 | 待用户验收 | V5.9.10 自注册 FARM_ADMIN 原子创建独立空农场；空农场可稍后添加首块持久化地块，当前 JWT 动态获得本场新地块权限且跨农场严格拒绝。 |
| T-138 | 待用户验收 | 受控学习质量门：确定性评估器维护 `PENDING/QUALIFIED/REJECTED`，仅合格案例进入正向检索/策略候选；不合格案例保留为反例和审计记录，候选必须离线验证并人工批准，接口按会话、账号、农场和地块隔离。 |
| T-142 | 待用户验收 | 农户操作系统提供“灌溉系统 / 光照系统”子系统切换；灌溉保留土壤湿度曲线、灌水预警和补水执行，光照展示强度曲线、目标带、低/高告警与虚拟补光；同时提供 AI 直答模式：兼容模型单次生成且关闭思考，模型可用时普通问题直接由模型组织回答，不再以规则快捷答复替代，受控写操作和模型故障仍由确定性安全/降级路径处理；三入口及聊天模块缓存版本已刷新，避免旧缓存继续显示旧来源。 |
| T-143 | 待用户验收 | V5.9.11 将登录页、三角色网页工作台、备用登录页及微信小程序的系统文案统一为中文；动态枚举与后端错误使用中文展示兜底，保留内部编码和原始调试载荷；删除工作台字体选择并迁移旧偏好为系统默认，主登录页保留原仿宋视觉，`agriloop` logo 保持不变。 |
| T-144 | 已完成（本地验收） | 农户消息中心未读消息显示红色圆点，打开列表消息或首页最近动态中的单条消息即静默标记为已读；进入消息中心不批量清除，详情页移除手动“标记已读”，已读 ID 按账号与正式/演示模式保存在浏览器并可在刷新后恢复。定向 Node 13/13、语法检查、Vite 62 modules、桌面/390px 本地浏览器功能与视觉验收通过；不改后端消息接口、告警/任务状态，未部署线上。 |

界面改动在代码、自动化测试和前端构建全部完成后统一进行一次浏览器功能与视觉验收；可以保留必要截图和功能清单，但不在开发过程中反复截图。优先复用并刷新已有本地页面，验收结论确认后再同步待验收任务状态。

## 5. 最近验证

| 验证项 | 结果 | 备注 |
| --- | --- | --- |
| 2026-09-02 V5.9.11 全界面中文化收口 | 通过（本地） | Web Node 148/148、Vite 生产构建、全部改动 JavaScript 语法、`git diff --check` 和微信小程序静态校验（6 个页面、11 个 JS 文件）通过；浏览器检查主登录/注册 390px、备用登录 390px、农场管理员 9 个导航页、系统管理员 9 个导航页及农户首页/助手，未发现禁用英文系统文案、全局横向溢出或 warning/error；主登录页确认保留仿宋，工作台无字体选择。Java Gradle 在测试执行前被本机 `Unable to establish loopback connection` 阻断；未安装微信开发者工具，因此小程序仅完成源码和静态校验。 |
| 2026-09-02 T-142 农户操作系统子系统与缓存修复 | 通过（本地） | Web Node 107/107、JS 语法、Vite 生产构建和 `git diff --check` 通过；浏览器确认灌溉/光照子系统切换、光照强度曲线、7 小时/1 天/7 天模式按钮、补光控制和 390px 无横向溢出，控制台无 warning/error。 |
| 2026-09-02 T-138 受控学习质量门 | 通过（静态门禁，待定向验收） | Java 主源码/测试编译、4 个前端模块 `node --check`、`git diff --check` 和冲突标记扫描已重跑；完整测试套件未运行。远端最新 `main` 的业务更新以远端版本为主体，受控学习仅作增量补回，并已随 `main@dfc1dbe` 发布。 |
| 2026-09-02 T-142 AI 直答模式 | 通过（静态与线上服务核验，待定向验收） | 已核对模型调用仅保留一次直答请求、`chat_template_kwargs.enable_thinking=false` 且不发送 `reasoning_effort`；普通问题、问候、模糊短句和能力问答在兼容模型模式下均进入模型生成，规则仅保留安全门与故障降级，`full` 别名也会探测并调用模型。三入口及聊天模块缓存版本已刷新；`git diff --check`、API 健康、模型 `/v1/models`、三入口 HTTP 和登录后 Agent（`adapter=openai-compatible`、`degraded=false`）通过，按用户要求未运行完整测试套件。提交 `dfc1dbe0e4d2b8ccf0d1f356a79b04e3d4af59ee` 已推送并部署到 `/srv/agriloop`。 |
| 2026-09-01 V5.9.10 自注册农场与地块接入 | 通过（本地） | 隔离真实 HTTP 使用两个新 FARM_ADMIN 验证独立空农场、零 Demo 地块/设备/遥测、首块地创建与修改、旧 JWT 动态授权、跨农场列表及修改均 403，并在文件型 H2 重启后读取同一农场 ID、地块 ID 和修改内容。Web Node 124/124、Java JUnit 89/89（主源码/测试直接编译）、Vite、JS、OpenAPI YAML 与差异检查通过；桌面与 698px 注册、空农场和首块地闭环无横向溢出，页面日志无 warning/error。完整 Gradle 被本机 Java NIO loopback 故障阻断。 |
| 2026-09-01 V5.9.9 受控三角色账号创建 | 通过（本地） | 隔离真实 HTTP 覆盖三角色注册、授权缺失/错误/限流、全局创建与列表、敏感字段隔离、越权拒绝、非系统账号状态/删除、系统账号永久保护及文件型 H2 重启持久化。Web Node 122/122、Java JUnit 77/77（主源码/测试直接编译）、Vite、JS、OpenAPI YAML 与差异检查通过；桌面和 698px 登录注册/全局账号管理无横向溢出，页面日志无 warning/error。完整 Gradle 被本机 Java NIO loopback 故障阻断。 |
| 2026-09-02 T-117 农户人工浇灌入口、水量校验与布局 | 通过（本地，待线上） | Web Node 34/34；定向 Java 3/3（ASCII 驱动映射）；后端 `compileJava/compileTestJava`、`node --check`、Vite 62 modules 与 `git diff --check` 通过。浏览器在本地演示农户页面确认 1280px 灌溉建议改为单列、重复大湿度图不再渲染、人工浇灌按钮可见且可打开；390px 无横向溢出且入口仍可见；演示水库余额 720 L 时弹窗显示每日剩余 720.0 L、单次上限 270.0 L，无“水资源不足”误报。控制台仅有后端不可用时的预期演示降级 warning；未推送、未部署。 |
| 2026-09-02 T-132/T-135 灌溉证据门分级与补证闭环 | 自动化/构建通过，界面与线上未验收 | Web 定向 Node 17/17、ASCII 驱动映射定向 Java 3/3、`node --check`、Vite 生产构建、OpenAPI `swagger-cli validate` 和 `git diff --check` 通过；API `compileJava/compileTestJava` 通过。中文路径的 Gradle Test worker 仍以 `ClassNotFoundException` 结束，非断言失败；线上地址只读加载超时，本轮未登录、未点击虚拟浇水或部署，`.gitignore` 用户改动保留。 |
| 2026-09-01 main 合并与服务器发布 | 通过（服务级，待线上浏览器） | GitHub `main` 已包含合并提交 `165aefd9da72fc7f98eafe71eea5e89fe741e724` 及发布记录；服务器 `/srv/agriloop` 标记为 `165aefd9da72fc7f98eafe71eea5e89fe741e724`，数据库备份和旧应用回滚目录已保留，Supervisor/API 健康检查与公网静态入口均通过；登录后的双角色操作未执行。 |
| 2026-09-01 T-133 农户任务刷新与问题上报 | 通过（本地，待线上） | Web 定向 26/26，完整前端 109/109；Java Gradle 全量测试通过（使用 ASCII 映射路径规避 Windows 中文路径执行器误报）；Vite 构建与 `git diff --check` 通过。浏览器复用本地 farmer 页面验证执行中任务不再显示“开始执行”，并可打开“具体问题”表单；控制台仅有本地后端不可用时的预期演示降级提示。正式服务器尚未部署，未执行线上上报。 |
| 2026-09-01 T-134 农户端地块稳定排序与拖拽排序 | 通过（本地，待线上） | Web Node 114/114（串行测试避免演示存储并发干扰）、Java Gradle 79/79（ASCII 临时驱动映射）、Vite 生产构建、OpenAPI YAML 解析与 `git diff --check` 通过。浏览器在本地 farmer 页面验证长按拖动、一次保存提示、刷新后顺序保持、首页卡片/工具下拉一致、普通点击详情、十一项指标标签顺序、390px 无横向溢出和零控制台 warning/error。服务器尚未部署，线上页面仍需发布后验收。 |
| 2026-09-01 T-135 巡田记录与补证申请双角色可见 | 自动化通过，浏览器待补验 | Web Node 118/118；Java 直接 JUnit 81/81（Spring/H2 上下文、权限筛选、补证字段和拒写场景）；Vite 生产构建、OpenAPI `swagger-cli validate`、`git diff --check` 通过。Gradle wrapper 编译通过但测试 worker 在当前 Windows 路径下由 `@classpath` 误报 `ClassNotFoundException`，不是断言失败；本地浏览器已检查农户记录提交/刷新与管理员巡田区，但切换演示身份时被 URL 安全策略中断，双角色补证申请与移动端最终检查待补；线上需部署后再验收。 |
| 2026-09-01 V5.9.8 国内行情数据补齐 | 通过（本地） | 官方接口实测九品种 9/9 有当日价：重庆 5 种、全国后备 4 种；隔离 HTTP 首次 962ms、缓存 21–50ms。Web Node 119/119、国内/全球定向 13/13、Java 行情测试 6/6（直接编译运行）、Vite、JS 和差异检查通过；模拟趋势保持 `SIMULATED`、不可执行且不进入归档/统计。正式页面验证九卡、模拟开关、全球伦敦 DEFRA 隔离及 698px 无溢出，日志无 warning/error。完整 Gradle 因本机 Java NIO loopback 故障未运行。 |
| 2026-09-01 V5.9.7 国内行情本地化收口 | 通过（本地） | Web Node 117/117、国内/全球行情定向 11/11、Vite、JS 和差异检查通过。1440px 与 698px 正式 FARM_ADMIN 页面均只显示重庆归档和 7/30/90 日切换，国内 DOM 无“国际参考”、DEFRA 或英镑单位；全球批发伦敦节点仍展示独立 DEFRA `GBP/kg` 观测及“不进入到岸或毛差”说明。两种宽度无横向溢出，页面无错误面板。 |
| 2026-09-01 V5.9.4 国际参考历史曲线 | 通过（本地） | DEFRA 官方 CSV 筛选 62 个按公斤观测点并记录源文件 SHA-256、OGL v3.0 和转换说明；Web Node 111/111、Java 行情测试 5/5、Java 主源码/测试直接编译、Vite 与差异检查通过。隔离 18080 及正式 8080/3000 代理均返回 `LIVE`、`H2_STANDALONE`，本场番茄/黄瓜各附 17 期 `GBP/kg` 参考且本地历史仍各 1 日。浏览器确认默认国际参考、重庆/国际切换、12 期曲线、长期缺口断线、来源链接与原始单位；698px `scrollWidth=698`，控制台 warning/error 为 0。完整 Gradle 因本机 Java NIO loopback 故障未运行。 |
| 2026-09-01 V5.9 三账号真实资源同步 | 通过（本地） | Web Node 98/98、Java Gradle 74/74、Vite 生产构建、关键 JS/Python 语法和 `git diff --check` 通过。真实 HTTP 脚本以三个独立 JWT 验证需求提交、管理员排程、版本冲突、幂等确认、农户回执、系统管理员同 ID/版本/历史只读审计、跨农场/跨农户越权拒绝及三端 SSE；停止并重启文件型 H2 后同记录仍可查询。浏览器集中复核三个正式账号标签页、持久化状态标识和 SYSTEM_ADMIN 无写入口，三页控制台无 warning/error。 |
| 2026-08-31 管理员紧凑指标布局 | 通过 | 完整前端 71/71、`git diff --check`、Vite 构建通过；1180px 宽卡为 `4-4-3` 三行且高 379px，698px 中卡为 `3-3-2-3` 且由改前 813px 降至 490px，390px 极窄卡为单列。十一项均无靶标截断，三种宽度无横向溢出；详情打开/关闭正常，控制台无 warning/error，未部署服务器。 |
| 2026-08-31 管理员统一数据表面 | 通过 | 完整前端 71/71、`git diff --check`、Vite 构建通过；1440px 的 77 个指标块与 21 个事实块、698px 的各状态指标均计算为唯一背景色 `rgb(248, 251, 249)`，桌面仍为 `3-3-2-3`、窄屏仍为双列且末项通栏。两个宽度无横向溢出，详情可打开/关闭，控制台无 warning/error；未部署服务器。 |
| 2026-08-31 管理员总览边界强化 | 通过 | 未调整 DOM 与布局；完整前端 71/71、`git diff --check`、Vite 构建通过。1440px 实测首卡仍为 11 项 `3-3-2-3`，698px 保持原双列；地块、作物、事实、指标外框均为 2px，两个宽度无横向溢出，详情可打开/关闭，控制台无 warning/error；未部署服务器。 |
| 2026-08-31 管理员十一项指标卡 | 通过 | 服务器当前 11 个遥测编码与展示顺序已固化测试；`admin-state` 22/22、完整前端 71/71、Vite 构建通过。1440px 实测分行为 `3-3-2-3`，698px 为两列且末项通栏，两种宽度均显示 11 项、无横向溢出，控制台无 warning/error；未部署服务器。 |
| 2026-08-31 管理员 Emoji 作物身份卡 | 通过 | `admin-state` 21/21、完整前端 70/70、Vite 构建通过；698px 页面显示 7 个作物身份区且无横向溢出，控制台无 warning/error；服务器 `index.html`、`app.js`、`admin.css` 均返回 200 |
| 2026-09-01 管理员任务/告警/设置双服务器发布 | 通过 | 本地合并 `origin/main@bd3e578` 后 Web Node `99/99`、Vite 和差异检查通过；备份位于 `/srv/backups/agriloop-admin-ops-20260901-103607-a794e9e`。`/farm-admin/` 与 `/agriloop/` 均返回新缓存版本，告警选择门、逾期转交、纯色设置与十一项指标静态合同命中；公网 `/actuator/health` 200/UP。当前 Windows 界面自动操作连接不可用，本轮公网验收采用 HTTP/静态模块黑盒，未执行登录后的浏览器点击。 |
| `V5.8` 资源分配与风险推演重构 | 前端 Node `68/68`、Vite、OpenAPI YAML、浏览器通过 | 浏览器验证 AI 需求分析、逐行核对、容量试算与缺口、确认/取消状态、原因必填人工调整、地块干旱场景只读曲线、管理员/农户旧地址回落及浅色/暗色桌面页面；仅有后端未启动时的预期演示降级提示。Gradle 因本机 loopback 启动故障未执行断言。 |
| 2026-08-31 V5.8.6 管理员总览合并 | 通过 | Web Node 94/94、Vite 生产构建和 JS 语法检查通过；浏览器实测 7 张地块卡、7 个 Emoji 作物身份区和 77 项指标，桌面单卡分行 `3-3-2-3`，698px 收起导航后 `2-2-2-2-2-1`，无横向溢出，控制台无 warning/error。 |
| 公网 `/farm-admin/` 发布 | 通过 | `index.html`、`admin-alerts.js`、`work-order-lifecycle.js` 均返回 200 且命中新版本标识；API `UP`，正式管理员登录及任务/告警只读接口成功 |
| 2026-08-30 农户主面板本地回归 | 通过 | `farmer-dashboard-smoke.mjs` 11/11、Web Node 64/64、Vite 构建通过；本地浏览器验证宽屏/窄屏顺序、无横向溢出、双主题、任务/风险路由及控制台无 warning/error；详见 `docs/acceptance/FARMER_DASHBOARD_ACCEPTANCE.md` |
| 2026-08-30 农户端“农智助手”集中验收 | 通过 | Java Gradle 58/58、Web Node 67/67、Vite、语法检查和 `git diff --check` 通过；本地浏览器复用 farmer 页面验证桌面导航顺序、连续问答、依据展开、历史抽屉/新对话、巡田记录预览确认、READY 虚拟灌溉预览确认，以及 390px 窄屏工具栏换行、抽屉全宽和无横向溢出；详见 `docs/acceptance/FARMER_ASSISTANT_ACCEPTANCE.md` |
| 2026-08-30 T-096 应急补水与农智助手内联执行 | 通过 | API 59/59、Web 68/68、Vite、JS 语法、`git diff --check`；本地 3001 桌面验证聊天内“操作预览→确认→已完成/虚拟执行·模拟结果”、历史抽屉，390px CSS 宽度验证工具栏换行与无横向溢出，控制台未发现本地页面新增 warning/error；仍不触发真实水泵/阀门。 |
| 2026-08-31 T-108 农智助手状态/效果持久化修复 | 通过（本地） | 定向 Web 15/15、`node --check`、Vite 生产构建、`git diff --check`；复用本地 3001 农户页完成“生成灌溉预览→确认→切换我的地块→返回农智助手→刷新页面”，操作卡保持“已完成”、确认按钮不再出现，地块土壤湿度由 16.8% 回写为 40.7% 且刷新后保持，控制台错误/警告为空。 |
| 2026-08-28 服务器原地升级 | 通过 | 备份 `/srv/backups/agriloop-20260828-110354`；`main@601fe08` 同步两套目录，Supervisor 全部目标 RUNNING，API `UP`；服务器 API Gradle 52/52、bootJar 成功 |
| 2026-08-28 设备/绑定/告警/Agent 公网黑盒 | 通过 | 新建模拟设备绑定后 ONLINE/OFFLINE 幂等控制；设备详情与地块编辑跨地块转移；单条核查重复请求复用同一工单；Agent 创建地块确认、任务确认、取消、农户越权 403、正常核查自动关闭告警 |
| 2026-08-28 Agent 确认卡点击回归 | 通过 | 公网实时会话中确认/取消按钮初始均为可用；取消后状态变为“已取消”，确认后状态变为“已完成”，控制台无错误；确认更新历史元数据不完整的 `plot-a01` 成功并触发平台数据刷新 |
| 本轮任务/告警闭环 | Node `58/58`、Gradle、Vite、Web real/stub/svg `38/38/37` 通过 | 覆盖 7 条告警 AI 分流、批量核查任务、AI 运行中详情退出、逾期重新分配/处置后退出逾期页及五段导航；Edge 1440×1000 视觉检查通过 |
| 设备与设施控制闭环 | API Gradle、Node `59/59`、Vite、Python、Web real/stub/svg `41/41/40` 通过 | 覆盖来源标签白屏回归、模拟设备即时切换、真实设备 ACK 后更新、离线遥测/心跳保护、幂等/超时、卡片/详情开关；服务器部署后模拟设备离线→在线已恢复原状态，未操作真实 BearPi |
| 前端 `npm test`（中文化阶段） | `27/27` 通过 | 覆盖三角色共享数据合同、SSE 断线重连、快速遥测窗口，以及展示层中文状态/来源/服务映射；为历史记录，未在本轮重跑 |
| 前端 `npm test`（AI 告警阶段） | `36/36` 通过 | 新增派单/关闭本地状态强制补齐和列表计数实时更新回归；AI 告警可信度、农户选择、旧操作移除、对话布局和本地预览测试继续通过；为历史记录，未在本轮重跑 |
| `npx vite build` | 通过 | 2026-08-26 AI 告警与对话页收口后复跑；仅有项目原有的 Vue/ECharts 全局 vendor 非模块提示 |
| WebUI DOM 回归 | real/stub/svg `26/26/25` 通过 | 实际挂载农场管理员告警页并点击详情关闭，确认卡片立即从未关闭列表消失；同时检查重复下发入口移除、大标题移除、AI 对话页签切换和经典空白会话快捷问题可见性；为历史记录，未在本轮重跑 |
| Crop Pack 校验 | 最近记录通过 | 番茄、黄瓜配置与阶段规则已覆盖 |
| 后端 Gradle | 通过 | `./gradlew.bat :apps:api-service:test --no-daemon`（ASCII 联接路径）本轮 `BUILD SUCCESSFUL`；补充农户仅能为授权地块执行只读资源试算且不落排程记录的权限回归 |
| 浏览器功能与视觉检查 | 通过 | 2026-08-26 复用已有页面集中检查：仅保留一个“农务任务”主标题，页签、任务操作区、概况、筛选器和卡片区域左边缘一致；任务卡 Enter 打开详情、Esc 关闭，“待分配”和 `scope=overdue` 切换通过，控制台无 warning/error，并保留页面与详情截图 |
| 农户首页优先事项与建议闭环 | 通过（本地演示） | 2026-08-26 本地 Chromium 演示会话验证三项优先事项排序、任务详情入口、灌溉二次确认（地块/水量/时间/时长/湿度）、管理员审批反馈、结果表单、等待复测状态和地块详情六项曲线；正式后端与移动端仍待联调验收 |
| 系统管理员平台总览浏览器回归 | 通过 | 2026-08-26 通过 SSH 隧道加载远端正式会话：平台总览四项指标、6 个服务卡、模拟器状态、5 个地块卡片和事件摘要均可见；页面错误为 0，入口缓存版本已刷新 |
| 地块详情多指标曲线远端回归 | 通过 | 发布 `8c2c8c9` 后 SSH 隧道正式会话验证 8 项指标；每项历史末点与预测首点时间/值连续，重置预测后仍连续，页面无错误；`/actuator/health` 为 `UP` |
| 三角色实时刷新远端回归 | 通过 | 发布 `2f7e9a2` 后 SSH 隧道真实浏览器验证：管理员土壤湿度约 4.0 秒、农户代表指标约 4.9 秒、系统管理员地块指标约 2.4 秒内自动变化；详情曲线 10.5 秒内完成两个 4 秒刷新周期并保持 4 条序列；农户资源试算返回 200，三页无 4xx、控制台或页面错误。 |
| 三角色中文化与图标本地化回归 | 通过 | 2026-08-27 本地 Chromium 1440px 页面检查：农场管理员、系统管理员、种植农户主视图及动态内容均可见；本地图标字体正常，未加载远程 Material Symbols，页面无业务脚本错误。未连接本地后端时仅健康探针返回 500，属于预期的接口缺失提示，不影响演示会话渲染。 |
| 农户端 P0 创新闭环 | 通过（本地） | 2026-08-27：Gradle API 50/50、Node 40/40、Vite 与 OpenAPI 通过；复用本地农户页验证阶段目标带、8 类指标质量/不可用边界、三类证据、迟滞/冷却、双轨、审批工单、ACK/效果只读视图和 Agent 审计，390px 与桌面布局通过，控制台 warning/error 为 0。 |
| 农户地块展示收口 | 待验收 | 2026-08-27：Node 51/51、Vite 构建和静态检查通过；确认趋势图配置不含 `SOIL_EC`、批次阶段不显示 `I-19`、三处地块指标展示使用中文编码映射；现有 `file://` 浏览器页面刷新受安全策略限制，待 HTTP 本地地址复核。 |
| 农户灌溉自主执行规则调整 | 通过（本地+远端） | 2026-08-28：本地完成农户“查看建议并执行”四步闭环及 ACK/效果/幂等回归；远端 `e8187ed` 已发布，农户静态脚本含新按钮且无旧提交审批文案，`/actuator/health` 为 `UP`，农户执行权限为允许且审批权限为禁止，管理员两项权限均按规则保留；未在公网触发灌溉命令。 |

## 6. 阶段门

| 阶段门 | 当前状态 | 证据范围 |
| --- | --- | --- |
| Gate 1 / 数据可流动 | 已完成 | 1,000+ 事件、校验、去重、落库、推送、离线恢复 |
| Gate 2 / 闭环可解释 | 已完成 | 告警、诊断、就绪度、补证、处方、虚拟执行、ACK、评价和回放 |
| Gate 3 / 可答辩 | 后端与指定前端切片已完成 | PPT、录屏和最终答辩归档仍未制作 |

## 7. 当前证据入口

- 文档总索引：[`docs/README.md`](docs/README.md)
- 管理员综合验收：[`docs/acceptance/ADMIN_ACCEPTANCE.md`](docs/acceptance/ADMIN_ACCEPTANCE.md)
- 决策中枢验收：[`docs/acceptance/DECISION_CONSOLE_ACCEPTANCE.md`](docs/acceptance/DECISION_CONSOLE_ACCEPTANCE.md)
- 后端远端验收：[`docs/acceptance/REMOTE_ACCEPTANCE.md`](docs/acceptance/REMOTE_ACCEPTANCE.md)
- Web 性能验收：[`docs/acceptance/WEB_PERFORMANCE_ACCEPTANCE.md`](docs/acceptance/WEB_PERFORMANCE_ACCEPTANCE.md)
- 账号说明：[`docs/account-management.md`](docs/account-management.md)
- BearPi 适配说明：[`docs/hardware/bearpi-e53-ia1.md`](docs/hardware/bearpi-e53-ia1.md)
- 当前 AI 告警设计验收记录：[`design-qa.md`](design-qa.md)
- 农户端 P0 创新闭环：[`docs/acceptance/FARMER_P0_ACCEPTANCE.md`](docs/acceptance/FARMER_P0_ACCEPTANCE.md)

已完成的阶段分工、分支复核、旧 3D FarmMonitor 验收和历史截图已从当前工作树移除；必要时从 Git 历史读取，不再作为当前产品事实入口。

## 8. 当前约束

### 2026-08-30 本轮增量

- 历史图片分析曾使用浏览器端 ONNX Runtime + SqueezeNet/ImageNet；该方案只能产生通用分类候选，无法把图片交给服务端 AI，已由 T-108 的 Qwen 原生多模态链路取代。
- 本地后端新增农场级告警学习案例、策略候选、规则集和 Crop Pack 草稿/校验/启用接口，使用现有 `entity_record`，并将告警闭环接入案例记录；共享服务器后端尚未发布。
- Crop Pack 管理页增加“作物模型 / 告警规则 / 学习候选”分区和同尺寸“添加作物”卡片，农场管理员可保存草稿后再校验启用。
- 本轮仅允许独立图片静态资源发布到 `/srv/farm-admin`；不修改 `/srv/agriloop`、`/srv/system-admin`，不重启共享 API/Qwen，不推送 GitHub。

### 2026-08-31 本地管理员界面增量

- 农场管理员新增独立“规则与策略”导航，页面按当前 `farmId` 读取规则集和策略候选集；规则只读，候选支持批准并启用、拒绝和回滚等受控操作，操作后刷新告警与总览事实域。
- “农务任务 → Crop Pack”收敛为作物模型网格，移除重复的告警规则/学习候选页签；新增与普通作物卡片同尺寸的“添加作物”卡片和五步可视化表单，支持农场级草稿保存、继续编辑、校验和启用。
- 前端资源缓存版本更新为 `20260831-rules-strategies-v1`；本地 Web Node 回归 71/71、Vite 构建和 `git diff --check` 已通过。本轮只改本地代码，未部署服务器、未修改 `/srv/agriloop` 或 `/srv/system-admin`，未推送 GitHub。

### 2026-08-31 工作台设置增量（字体选择已由 T-143 收口）

- 农场管理员工作台设置增加简洁中性、田野绿、晴空蓝、麦穗金、果实紫五套主题预设，支持自选十六进制强调色和明暗/跟随系统并实时预览，将背景、侧栏、卡片、边框、按钮、表单和图表统一接入语义变量。V5.9.11 已删除工作台字体选择，备用登录页、三角色工作台和小程序固定继承系统默认字体，旧字体偏好自动归一；主登录页按最终视觉要求保留原仿宋字体，语言切换仍不提供。
- 设置升级为 `agriloop-user-settings-v2:<account>` 按账号隔离的浏览器本地存储，兼容旧版设置一次性迁移；非法颜色自动回退，告警/成功/警告等业务语义色保持不变。
- 图表监听主题、预设和强调色变化后即时重绘；系统主题变化可自动同步。系统管理员、农户端和登录页不做本轮视觉重构。
- 修复卡片风格被通用主题规则覆盖的问题：经典卡片、柔和玻璃、液态玻璃现在分别拥有实色平面、轻透明中等模糊、强透明渐变景深效果；农场总览地块、概况、告警、设备、成员、Crop Pack、任务和规则策略卡片均纳入切换，照片地块保留作物背景但同步改变圆角/边框/阴影；设置选项自身也提供三种可见预览。缓存版本更新为 `20260831-codex-appearance-v4`（管理员模块 `20260831-appearance-v4`）。
- 本地 Web Node 回归、Vite 构建、JavaScript 语法检查和 `git diff --check` 已通过；管理员工作台固定使用中文，保留用户自定义名称和指标原值；三种卡片风格切换不再弹成功提示。缓存版本更新为 `20260831-codex-appearance-v8`，本轮仅修改本地代码。

- 未经明确要求，不修改后端接口、JWT、Token、本地会话字段或角色权限边界。

### 2026-09-01 设备菜单与 AI 历史地块隔离（本地）

- 设备与设施卡片新增与地块卡一致的三点菜单；设备名称/类型可编辑，设备编号、所属农场和接入来源保持只读。永久删除仅允许离线、未绑定且无待回执控制命令的设备，并保留遥测、命令与审计历史。
- AI 助手移除历史栏重复收起按钮，保留工具栏无外框箭头；历史会话按当前地块查询和二次过滤，切换地块自动恢复该地块最近会话或新建空白会话；批量操作栏固定在历史列表顶部，已归档支持批量取消归档与删除。
- 新增 `PATCH/DELETE /api/v1/devices/{deviceId}` 与 `plotId` 会话列表参数，并同步 OpenAPI、演示 API、前端菜单和安全弹窗。Web Node `101/101` 与 Vite 构建通过；Gradle 因本机只有 JRE、缺少 Java 17 编译器未能执行，待具备 JDK 后补跑。
- 本轮仅修改本地代码，未部署服务器、未修改 `/srv/agriloop` 或其他服务器目录、未推送 GitHub。
- 未经明确要求，不推送、部署、创建分支或改写历史。
- 推送前必须先 `fetch` 并兼容远端新增提交，禁止 force push。
- 代码变更后执行相关测试和 `npx vite build`；文档状态必须与实际证据同步。
- 界面改动在代码、自动化测试和构建完成后统一执行一次浏览器功能与视觉验收，向用户提供功能清单和必要截图；复用已有页面，不重复打开标签。
- 当前默认是“软件仿真 + BearPi 真实遥测”混合交付；不能把单块 BearPi 输入适配扩大描述为生产现场端到端硬件系统，也不能把虚拟灌溉结果写成真实执行器动作。
