# T-075 设计与交互验收记录

> 日期：2026-08-26
>
> 范围：农场管理员“AI告警分析与智能处理”与“AI 对话助手”
>
> 当前结果：自动化功能与设计系统检查通过；真实浏览器截图对比待用户指定浏览器后完成

## 视觉基线

- 以已合并的 `origin/main@0349700` 和管理员 v5.6 现有组件为唯一视觉基线。
- 告警页继续使用 `auto-fill/minmax(280px, 1fr)` 白色概要卡、语义色边框、紧凑标签和居中详情弹窗。
- 不引入新的背景图、装饰渐变、手绘 SVG 或假图标；按钮图标复用项目 Phosphor 映射。
- AI 对话页使用同一套白色表面、语义色和字体栈；正文与输入字号为 16px。

## 核心流程检查

| 流程 | 结果 | 证据 |
| --- | --- | --- |
| 全选当前告警 | 通过 | Vue 挂载后批量工具栏和选择状态可见 |
| AI 分析后下发 | 通过 | 唯一 AI 入口复用工单创建/分配合同，优先地块权限、低工作量和地块经验 |
| 批量关闭 | 通过 | 复用告警 close 合同，成功/失败分开提示 |
| AI 智能处理 | 通过 | 80% 阈值、缺失证据、候选规则和诊断不可用均有回归覆盖 |
| 不确定性保留 | 通过 | 不达阈值、无合格农户或诊断失败时不自动下发 |
| 完整 AI 对话页 | 通过 | Vue 挂载后可从告警页切换；新对话后 4 个快捷问题固定显示在输入区上方 |
| 旧操作移除 | 通过 | 农场管理员告警 DOM 不包含“确认收到”、“升级处理”、“转成任务” |

## 自动化结果

- Node 前端测试：36/36 通过，包含派单/关闭本地状态和本地预览回归。
- Vite 生产构建：通过；只有项目既有 Vue/ECharts vendor 非模块提示。
- WebUI 挂载回归：real 25/25、stub 25/25、svg 24/24 通过，包含新对话快捷问题可见性。
- `git diff --check`：通过。

## 未完成的视觉证据

用户提供的公网 URL 截图在页面启动幕处停留，不能作为管理员告警内容的有效截图基线。用户尚未指定 Chrome 或 Edge，因此本轮不擅自运行真实浏览器截图比较。在用户确认前，T-075 保持“待验收”，不将自动化 DOM 结果冒充为最终视觉签收。
